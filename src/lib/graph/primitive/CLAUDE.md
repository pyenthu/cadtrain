# `primitive/` — part-source execution

Runs volume part sources: the sandbox that injects helpers (`primitive-sandbox`),
typed-create scaffolds (`primitive-stub`), client GLB export (`glb-client`), and
the WASM Manifold-trap guard (`manifold-trap`) that stops one bad bake poisoning later ones.
