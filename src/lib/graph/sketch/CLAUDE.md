# `sketch/` — M.1 sketch engine

`compileSketch(ops)→(r,z)` over Maker.js (line/spline/fillet/chamfer), injected into
the part sandbox as `sketch(...)`; plus sketch column layout and repeat expansion.
Plan: `docs/plans/profile-sketcher.md`.
