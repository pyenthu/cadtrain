# `expr/` — expression + formula language

Parse / validate / emit for graph expression blocks (`graph-exprs`, `expr-schema`),
imperative accumulator programs (`expr-imperative`), loop parse/serialize
(`expr-loops`), the sandbox/profile math library (`math-lib`), and TrueForm WAT
emit (`tf-wat-emit`).
