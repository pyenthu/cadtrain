Build a software architecture overview app.

A level-1 heading and a muted subtitle, then a **tabs** panel with two tabs — **Tree** and
**C4** — each a `nodetree` graph (nodes + edges laid out left→right by parent depth). Seed
`nodes`/`edges` for the architecture tree (one system → containers → route/api/lib/store
components, some archived) and `c4nodes`/`c4edges` for the C4 system context (person → system
→ externals). Light theme, slate accent.

Components: heading · text · tabs · nodetree (nodesVar/edgesVar → seeded vars).