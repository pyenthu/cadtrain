# TODO — crisp. Full detail in `/plan` (Gantt) + `docs/plans/*`.
> **Execution:** `batches.md` groups these into issue-and-run batches ("run Batch N to completion").

### Active focus
- **#940/#22 Modularize GraphEditorPane** — Phase 4 (inline, browser-verify each cut): pull part-actions · bake-orchestration · expr-actions · keymap/save · canvas-interaction · CSS onto `controller.svelte.ts` + a `GraphCommand` labeled-undo layer; NodeCard per-type split. (`graph-editor-pane.md`, `modularize-round2.md`)
- **#7 Wells `/wells`** — register `g_*` completion parts into the parametric registry; long-string perf; real wells store. (`well-schematic.md`)
- **#42 Wells interface (SVTC WsonApp)** — W-B editor/inspector dock · W-E left toolbar rail. (`wells-interface.md`)
- **#42b Wells → ewells.app PARITY** (`wells-ewells-gaps.md` · `wells-build-architecture.md`):
  - [ ] **A · BUILD ARCH (3D-fast)** — WellBakePool (per-element parallel bake) · clip-plane cutaway · element libraries · auto-scale/fit 2D+3D (dia/depth exaggeration) + manual Dia×/Depth×. (P0–P5)
  - [ ] **B · EDITING (the differentiator)** — mutation+undo · CompletionsEditor · SurveyEditor (live re-warp) · inspector-on-select · edit-diagram popovers.
  - [ ] **C · RENDER polish** — 3D center/fill · label banks + anchor · white bg · element-rail icons.
  - [ ] **D · CHROME** — toolbar actions · ruler MD/TVD · camera presets.
- **#42h Wells 3D bakes through the PRIMITIVES pipeline — CORE DONE, cleanup LEFT** (verified 2026-07-28). The /wells 3D tab already bakes `wsonToGraph(wson)` via `GraphEditorPane` (`seedGraph`) → client-worker Manifold, per-part `parts[]`, NO-FALLBACK error panel; the SVTC pure-THREE shell is unmounted (the `WellBakePool` seam ended up UNUSED). **LEFT:** (a) a wells `3D BAKE | TF` engine selector + real TF wiring, (b) rail per-element colour/layer toggles reaching the 3D pane, (c) DELETE the dead files — `WellSchematic3D.svelte` · `WellScene.svelte` · `threeD/manifoldCut.ts` · the dead WellBakePool seam. Memory `wells_graph_bake_units_and_parts`.
  - ⚠ **Render `parts[]`, NOT the composed body** — `M.compose` UNIONS overlapping bodies (the well collapses to the outer hole). Fixed at root `b3f8b14` (lazy `_parts`).
  - ⚠ **Mixed units: radial=INCHES (`od`), axial=METRES (`length`/`Mv`)**; `bw_*` defaults are inches — an axial param must be passed or it bakes ~39× long. Memory `wells_graph_bake_units_and_parts`.
- **#42g Wells autoscale + directional/straight toggle.** ⚠ **Use SVTC's names** (already upstream): `xDiaScale` · `yScale` (depth, 0.17) · `autoScale` · `directional`. ⚠ **They persist in WSON as `meta.display` — `wson.ts` doesn't model it; extend the model, don't build a competing path.** No auto-scale exists today (grep `autoScale|autoFit` → 0). Want auto-fit on load/change + manual override (part of #42b A). Promote `settings.directional` (`view-settings.ts:38`, buried in the display popover) to a top-level segmented control beside `2D|3D|GRAPH`.
- **#42c · Recreate the completion/well element parts on the VOLUME** (under `well_parts/`, referenced by id — feeds #77's no-parser model). Parametric `bw_*` (od/id/length-driven): `bw_packer` + tubing, then the rest; port the fixed `g_*` profiles (compjson revolves, empty params) into parametric parts. ⚠ **WRITES THE SHARED PROD VOLUME (`/api/primitives/save`) — never hand to an unattended agent** (memory `subagent_shared_volume_2026-06-14`).
- **#42d Wells CAD-parts browser + WBG Wizard** — model on AIDE WBG Wizard (`docs/plans/refs/aide-wbg-wizard.png`): section-by-section WBG (hole/casing rows, API-size pills) · CAD-parts picker (drop a `/primitives` part with `meta.params` GUI + depth anchor/string/orientation) · left nav (WBG·Tubulars·Rig·Trajectory·Summary) · Auto Design (trajectory gen) · toolbar. (`docs/plans/wells-cad-parts-browser.md`)

- **#43 · CHAT→WELLS AI SYSTEM — RAG pipeline + training/correction log (deep-dive SVTC first)** (user 2026-07-13). **LONG-TERM VISION:** a chat interface where a user prompts and the system CREATES/EDITS wells, powered by OUR OWN LOCALLY-TRAINED model (prompt→WSON/graph). The pieces to build the training + grounding infra now:
  1. **Deep-dive SVTC's AI system** (`~/code/SVTC/src/lib/ai/`: `chat.svelte.js` · `ChatPanel.svelte` · `rag.js`+`ragIngest.js` · `systemPrompt.js` · `tools.js`+`toolSchema.js` (function-calling → app mutations) · **`trainingLog.js` = the "wrongresponse" register** · `webllm/` (LOCAL model) · `contexts/` · `tasks/` · `API.md`+`CLAUDE.md`). Understand the full loop: chat → RAG-grounded prompt → tool-calls that mutate the app → correction capture → local-model inference.
  2. **RAG pipeline for chat — EXTEND, don't rebuild.** cadtrain ALREADY has `src/lib/server/{rag-corpus,rag-l1,rag-query,rag-prompt}.ts` + `/api/rag/{assist,prompt}` + `src/lib/shared/graph-editor/ge-assist*.ts`. Map what exists vs. SVTC vs. what chat→wells needs (retrieve over `bw_*` parts, vocab, wson samples, docs).
  3. **Training/correction LOG (the register), broader than SVTC's:** capture, as training data —
     - chat prompts + responses + **wrongResponse corrections** (SVTC `trainingLog.js` pattern);
     - **well EDIT tuples: `(prompt/instruction, well_before, well_after, DIFF)`** recorded on every enhance/modify (user 2026-07-13 — "if I enhance and get a modified well that should also be recorded") → supervised data for prompt→well-EDIT;
     - **full-well SNAPSHOTS** + provenance (the corpus of complete wells).
  4. **LOCAL model path** = `webllm` (memory `todo_webgpu_slm` · SVTC `webllm/`), per the BINDING **local-first AI** constraint (`ai_data_residency_local_first`): runtime AI is in-browser; cloud Claude = dev/authoring only.
  ⚠ **Persistence** = the volume (Rule 4/13 atomic writes, never clobber); a SUBAGENT must NOT write the shared PROD volume — verify any store against the LOCAL `.dev-volume` only. First safe increment (headless): the register store (append/list/diff) + RAG-for-chat retrieval reusing the existing corpus + vitest; NO speculative chat UI, NO live-LLM wiring yet. Cross-ref #0 AI · #42d WBG wizard · `todo_ai_function_mapping`.

### Open — engines
- **BREP display-mesh T-junction FLICKER — occasional, minor** (user 2026-07-12: "slight random flicker, not too bad"). OCCT tessellates each face independently → adjacent faces get mismatched edge samples → hairline z-fighting on shared edges (a known cosmetic BREP cost — `engines/brep/CLAUDE.md`). NOT a self-intersection (that `sweep_tube_demo` throw is fixed, `666086d`). LOW PRIORITY. Fix path: stitch/weld the OCCT triangulation across face boundaries (shared BRepMesh vertices) or a small display-mesh vertex-dedup at emit; verify by DECODING coincident/near-coincident edge verts, not eyeballing.
- **BREP renderer — interpolate vertex normals for material shading** (user 2026-07-12). Smooth per-vertex normals for the BREP material, not flat facets. **E5** (`39757ed`) already computes crease-aware corner normals shared by TF+BREP (`crease-normals.ts`) — verify they REACH the BREP material path (`PrimitiveDualCanvas` flatShading gate + the BREP mesh path) and interpolate across the OCCT triangulation so curved faces read smooth. Sibling of the T-junction flicker (both are OCCT display-mesh quality). (`/plan` #993)
- **BREP renderer — per-part / nested colours NOT rendered (OPEN BUG)** (`/plan` #997 — was mis-marked done). Cut-colour parity (`colorOuter`/`colorInner` on the section) shipped headless-only (`eb75b56` · brep 11/11), but the VISUAL is wrong: a nested/multi-part BREP part renders flat — `bw_packer` shows all-brown, the blue seal + per-subpart colours missing (screenshot 2026-07-28). ROOT CAUSE: `__tagNest` (the per-subpart colour tagger, #947) is **MF-only** — on BREP it aliases `__tag`, so nested colours flatten. FIX: implement per-subpart colour tagging in the BREP executor (or a shared LUT the BREP mesh path honours). Sibling of #993 (normals).
- **BREP client-side bake (E2/#39).** BREP is a SERVER OCCT singleton (`brep-occt.ts`, `resetOC()` on trap) — not parallel-safe. Target = the Manifold/TF split: server compiles the recipe, a **pooled** client Web Worker builds+meshes (WARM pool of N OCCT workers — init is multi-MB; per-part trap isolation for free), then cutaway via OCCT `.cut()` half-space (`todo_brep_cutaway`). GUARDRAILS for overnight agents: verify HEADLESS (OCCT runs in Node); NEVER write the shared prod volume. (`docs/architecture/geometry-engines.md`)
- **LEFT — SVG tab is the LAST browser-facing `/preview` caller** (`RightPane:335`): needs `segmentsFloor` (not in the client worker) → port to `bake-worker-core` then move the SVG bake onto the worker. Headless server-bake callers (`vocab/{infer,bake-proposed,regenerate}`, `ProfileFn3DCanvas:59`, the `🔄` path) still hit `/preview` — run them off-thread (`worker_threads`).
- **#42f · Port real geometry into 4 placeholder well elements** (`well_parts/imported/{bw_pup_perf,bw_trsssv_sp,bw_gauge_mandrel,bw_packer_ahr_ahc}`, plain bored tubes). ⚠ **Do NOT alias the `g_*` parts** — they are compjson-inferred revolves with EMPTY `params` (FIXED geometry; `g_trsssv_sp` bakes 3.56″×11.1″ regardless), while the translator feeds every element `od`+`length` → aliasing bakes the wrong size at the wrong depth. Port = lift each `g_*` profile into a **parametric** revolve driven by `od`/`length`. Drop from `PLACEHOLDER_PART_IDS` (`wson-to-graph.ts`) when done (`wson-to-graph-coverage.test.ts` enforces).
- **#65 Radial-scale + Z-scale as real PARAMS (warp-aware).** Move `xScale`/`zScale` from render-time scene dials to **build-time per-part params** so exaggeration survives bake/export (`feedback_expose_dont_hide`). ⚠ **Under a warp, Z scale applies along the spline's ARC LENGTH, not world Z** — scale the arc-length coord `s` before the frame lookup (`warp-spline.ts:11,196`); world-z would shear a lateral. Both engines. Headless-verifiable (bbox + arc-length monotonicity). **LEFT:** UI not wired (no spline-mode toggle in `SplineEditorPopup`, no warp-scale sliders; model+emit+bake shipped `46b29fe`).
  - [ ] **N5 (held)** curvature-adaptive subdivision — the J-well tangent section is straight but still gets rings. Blocked on N6.
  - [ ] **N6 · Adaptive meshing must be UNIT- and SCALE-aware.** Sag tolerance is `0.4% of the largest RADIUS` (inches) while curvature `R` is metres (the mixed-unit split); and sag must be judged in the **rendered, scaled** frame (a depth-exaggerated well makes a "smooth" 1 m sagitta invisible). Needs: unit tags per length, render transform (`zScale`/DTX/fit) reachable at BAKE time (Rule 25 — build-time, not post-hoc), and a fraction-of-view sag budget. Files: `axial-density.ts` · `bake-worker-core.ts` · `scene-state.svelte.ts` · `buildRemap`. (`/plan` #944)
  - [ ] **N6a · A SCALE is a set, not one number** — `radial_scale`·`x`·`y`·`z`·**`z_axial_scale`** (depth along the trajectory arclength ≠ `z_scale` for deviated wells; the ruler + sag budget read the axial one). In practice we drive `radial_scale` + `z_axial_scale` (fat-and-short schematics). Anisotropy ⇒ circle→ellipse ⇒ sag must use the scale in the curvature's direction. Lean bake-input for the axial scale, render-only for pure viewing stretches.
- **SVG smooth shading — reuse the 3D shading, don't fork a renderer** (`/plan` #985). ⚠ **HARD CONSTRAINT (user 2026-07-10): extend `PrimitiveSvgView`+`svg-emit`, no parallel path.** RESEARCH first. Concrete path: Lambert-shade each face from the SHARED crease-aware corner normals (E5's `creaseAwareCornerNormals` — one normal source for all 3 renderers + SVG); flat per-face fill (cheap) vs per-vertex `linearGradient` (smooth). ⚠ **gradient ids must be namespaced** (`svg_gradient_id_collision`). Back-face cull by projected normal sign. Folds #63 + SVG-projection-smoothness.
- **IDEA · a `BREP_SVG` tab — shade the SVG from BREP boundary SURFACES, not the mesh** (user 2026-07-12). BREP is a *boundary* representation: exact faces + face topology, so the face boundaries ARE the silhouette/crease outline and each face carries an exact analytic normal — a natural fit for a "shaded path on surface" SVG (clean true-edge outlines + smooth per-face/per-region fills) rather than projecting a triangle soup. Complements #985 (that shades mesh facets; this emits smooth region fills bounded by real edges). New right-pane tab, server-side (OCCT in Node, like the BREP tab). RESEARCH first: OCCT `HLRBRep_Algo`/`HLRBRep_PolyAlgo` (hidden-line removal → visible + hidden edges as 2D curves) for the outline; per-face normal → Lambert fill for shading. Cross-ref #985, `docs/plans/svg-projection-perf.md`, archived `exporter.ts` (VisibleChainPass).
- **✅ #996 · cement = a DERIVED annulus — DERIVATION SHIPPED** (`137c5ad`, merged 2026-07-13 · wells 400/400). `deriveCementSegments()` (`wson-to-graph.ts`) builds radial boundaries from casing/hole walls, clips each cemented string to `[TOC, shoe]`, segments at every breakpoint, takes the nearest boundary radially outside the casing OD per segment, merges equal-OD runs, tags NORSOK annulus letters (A reserved for tubing annulus) + barrier roles; emits one `bw_cement` Call per segment. WSON gained an optional `CasingString.cement` spec (`toc`, spans, `BarrierRole`), additive beside the legacy `cementing[]`. Cased-lap verified (2 annuli: against-casing then against-openhole). Algorithm/grounding: `docs/research/cement-annulus-detection.md`. LEFT: render/colour the annuli by barrier role (blue/red per NORSOK WBS) in the 3D+SVG (ties to #997 colour work); wire a cement-edit popover (#42f). Derived — recomputes when the casing program changes. Never wrote the volume.
- **✅ BUG · MF_CLIENT bake badge `fresh · 0 ms` — FIXED** (`/plan` #987). Core wiring already shipped (`0d873ce`: `onBakeTimings` `PrimitiveDualCanvas → RightPane`, compile+bake shown separately, cached = IndexedDB bake-cache hit). 2026-07-13 (`7b9a03b`) added the missing tested seam: pure `bake-badge.ts` (`sumBakeTimings`/`bakeBadgeTotals` + 7 tests) and fixed the tooltip which had silently OMITTED the `finalize` phase from the sum. ⚠ **VISUAL confirm still owed** — eyeball the badge shows real ms on a multi-second bake.
- **#46 Graph→TF compiler** — two-tier recompile on a STRUCTURAL hash (topology + param NAMES, not values) so param edits stay real-time; per-SUBPART material (needs color-by-source). (`tf-compile-perf.md`)
- **#51 TF welded-mesh builder** — #50 + NURBS smoothing (`tf.mesh`, `taubin/laplacianSmoothed`).
- **TF · add a native `r_loft` builder** (user 2026-07-12: `g_barrel` → *"not built natively — TrueForm has no native builder for: `call:r_loft`"*). `graph-to-tf` lowers r_cuboid/r_revolve/r_weld_extrude/r_sweep but NOT r_loft, so every barrel/waist/flare/ogive/scurve part blanks on TF (native-only, no MF fallback). Add `op:'loft'`: build the section stack from the SAME `scaleAt(t)` shape curve + twist that BREP's `r_loft` uses (`brep-occt.ts` — per-t scaled+rotated sections; `r_loft.ts` is the source of truth) and weld/loft like the MF gridPatch. Executor: a `tfLoft` in `tf_examples/execute.ts`. Verify HEADLESS (bbox/volume vs MF oracle — a barrel is fat-in-MIDDLE, so a monotonic endFactor can't fake it, same pin as `brep-occt.test.ts` t_barrel).
- **TF has NO geometry cache — every bake rebuilds.** MF caches at 3 layers; TF has only the warm kernel. Give TF an IndexedDB mesh cache keyed on the recipe hash (+cutaway+params), mirroring `bake-client.ts`. Needs a stable `TfRecipe` hash (canonicalize the JSON). Matters for /wells (N concurrent element bakes).
- **`compose` toggle (separate vs fused parts)** — default is PARTS everywhere (`5a94a10`). LEFT: expose the OPT-IN — an explicit per-part/list `MF_Compose`/`weld` that fuses to one solid when intended (a real union). Mirror in TF.
- **Manifold↔TF bake consistency** (`docs/findings/manifold-vs-tf-audit.md`) — (a) converge warp densification (MF fixed `WARP_AXIAL_MAX_ZSPAN` → curvature-adaptive like TF); (b) share graph-lowering primitives (`consumed-set`/`stack_ref`/`poly_repeat`) between `graph-to-tf.ts`+`composition-emit.ts`; (c) port bore-extend defect-2 prevention to MF hollow sweeps. ⚠ dedup `creaseAwareCornerNormals` (`render-helpers:529`≈`trueform-adapter:271`) is RISKY (differ by a `weldTol` param, not a safe delete).

### Engines — modularize + BREP parity
- **E1 LEFT** (engine modularization `engines/{brep,trueform,manifold}/` shipped `da7399f`, in the header ledger): (a) `engines/types.ts` common `Engine` interface — DEFERRED (design WITH user); (b) `stdlib/` stays in `graph/` (globbed by engine-hash + `server/stdlib.ts` — separate task). Next = **#16** (modularize `shared/`).
- **E2 · BREP full 3D-bake parity: server COMPILE → CLIENT worker CREATE + cutaway. (Overnight-scoped.)** See "BREP client-side bake" above. New `engines/brep/{worker,client,core}`; split `server/brep-occt.ts` (pure-recipe vs worker-exec).
- **E3 · Warm POOL for the MF + BREP client workers (not a lone singleton).** Unlocks concurrent multi-part bakes (wells) + per-part trap isolation. Model on WellBakePool (`wells-build-architecture.md` A1).
- **E4 · BREP-native WARP = a true spline sweep (own night — the deviated-well prize).** Mesh-warp on a BREP solid throws away the exactness; the native path is `MakePipeShell`/`ThruSections` along the survey spine → a curved B-rep. Subsumes BREP gaps #3 (warpSpline no-op) + #5 (deviated revolve). Verify HEADLESS (straight survey ≡ un-warped revolve; curved → valid single solid tracking the trajectory). Depends on E2.
  - **✅ PROVEN 2026-07-12 (branch `worktree-agent-a5dc3020`, HELD).** `w_deviated_casing` → 2232 tris, vol == MF oracle 120.6 exactly. ⚠ **THE CUT = section-BEFORE-sweep** (`bw_casing`'s 180° cut baked into the profile; OCCT `.cut()` on the CURVED solid is WRONG — removes 67% not 50%). **LEFT for full E4 (`/plan` #988):** wire into the real bake pipeline · curvature-adaptive spine density · `stretch:true` · high-torsion RMF roll parity · quarter-box cutaway on a warped solid.
- **E5 LEFT** (smooth BREP render shipped `39757ed`, in the header ledger): MF's `render-helpers.ts` copy stays separate (exact 1e-4 weld vs tolerance-union; MF normals pinned byte-identical by goldens — not a safe delete).
- **Curvature-adaptive axial meshing — ALL THREE engines** (`Δz=√(8ε/κ)`): MF `warp-spline`/`r_revolve` · TF `planAxialStations` · BREP `nSpine` (#988). Shared `κ(z)→Δz` model. Plan: `docs/plans/curvature-adaptive-warp-subdivision.md`. N5/N6 are the MF slice.
- **BUG · BREP throws on curved hollow SWEPT-BOOLEAN solids** (`s_tube`, `s_tube_copy`). A curved `r_sweep`+`.subtract`/bore = coincident tilted end-caps of two coaxial curved sweeps (r_sweep defect-2; `r_sweep_normals_and_twist`). (`sweep_tube_demo` — a SINGLE sweep — now bakes via the loft recovery `666086d`; this remaining case is the boolean/bore family.) MITIGATION: **annular section** — CSG in 2D on the section + sweep ONCE (`docs/plans/annular-csg2d-section-sweep.md`). Until then degrade to the uncut solid, not throw.

### Open — editor
- **#16 Modularize `src/lib/shared/`** — 50 loose root files / 12.3k lines. Pure `git mv` + import rewrite, ONE sweep. Subdirs (from actual imports): `viewer/` · `svg/` · `engines/` · `profiles/` · `types/` · `volume/` · `ui/`. Two leave `shared/`: `WellSideNav`→`wells/`, `anthropic-api`→`server/`. ⚠ **`src/routes/design/folder-tree.ts` hardcodes 113 `shared/…` paths — update in the SAME commit** (breaks the /design diagram with no compile error). Verify: build + test + open /design Folder tree. (`todo_modularize_k65`) **NOW ALSO: the graph↔shared audit (`docs/research/graph-shared-overlap.md`) says move `shared/graph-editor/` → `graph/editor/` FIRST — it kills 76 of 77 layering violations + the import cycle; do it as its own commit.**
- **#995 Modularize `src/lib/graph/` by ABSTRACTION** (user 2026-07-12). 39 loose top-level files → subfolders by concern: `composition/` (9) · `sketch/` · `spline/` · `expr/` (3 each) · `sweep/` · `survey/` · `wire/` · `warp/` · `profile/` · `primitive/` · `port/` · `part/` · `csg/` + existing `nodes/`/`stdlib/`. Mechanical `git mv` + import rewrite in focused commits (mirror #16 + E1). ⚠ update hardcoded paths (`routes/design/folder-tree.ts`) same commit; ⚠ archive/ twin-dir sed hazard (`bulk_path_rename_archive_twin_hazard`). **TESTS: ONE `graph/tests/` for the whole module** (consolidate `nodes*/tests` + `stdlib/tests` in; NOT per-subfolder). Each subfolder gets a short CLAUDE.md. Payoff: navigability + faster code search. (`/plan` #995)
- **#52 Modularize RightPane** — extract per-tab bodies (SVG/GLB/TF-recipe); large component → HMR skips it.
- **Material: graph-WIDE default chip on the OUTPUT card** (follow-up to #66/#982, shipped `6bd730d`). The per-part material-on-card work landed — each Call card has a material swatch chip → anchored popover (colour/material/texture/opacity) + the pre-existing ◑ wire hookup, and PROPERTIES is gutted to z-off. But removing the appearance table also dropped the graph-WIDE DEFAULT editor (was the "Default" row), so un-overridden parts fall back to the built-in red/grey with no UI to change it. Add a "default ◑" chip on the output/root card that edits `graph.colorOuter/colorInner/material/opacity` so a base colour/finish can be set once for all parts. (Also open: per-SUBPART material #947.)
- **#61 Material system + MATERIAL CARD** — (b) textures via `meta.texture`; (c) per-PART+SUBPART surface as the #66/#982 node-card popover (NOT a 4th card). (`wells-ewells-gaps.md` §G-MAT)
- **#63 SVG bake ↔ material** — (c) SVG `<pattern>` textures. `svg-emit.ts` · `PrimitiveSvgView.svelte`.
- **#38 Data-driven params — P2 · list<record> table editor** — `ParamsCard` "add object/row" builds a `list<record>` inline (strings table); the well rebuilds with N strings, zero card-wiring. Payoff: `w_multi_string_dev` 18 cards → 1 list param + 1 producer.
- **#38c PARTS-TABLE data INPUT socket (top-left) — data-driven rows from a wired `list<record>`** (user 2026-07-13, discussed). Add an optional INPUT socket on the parts_table title-row LEFT edge that accepts a `list<record>` — from a function/expr OUTPUT socket OR a literal list-of-objects node. When wired, EACH list element becomes a row (columns bind to record fields); inline authoring stays for the hand-made case. **This unifies parts_table with `parts_map`** (which already maps a `list<record>` param through one arg-map to N instances) — same `computeListProducers` + per-element instantiation path; parts_table = inline-authored rows, parts_map = data-driven rows, this bridges them. **The external-app seam:** an app / AI / function can inject a JSON array of `{od,wall,…}` records into the input and get N parts back (aligns with the local-first app-embedding direction). Node-kind changes: `sockets()` gains an input (`data`, typed `list<record>`); `inputRefs` includes the wired source; emit switches to a parts_map-style `Array.from(dataExpr, (row) => src(row))` when `data` is wired. **DESIGN DECISION to settle at build:** wired data SUPERSEDES inline rows (rows → read-only preview, default — mirrors the "wired aggregate supersedes per-row sockets" rule) vs. APPENDS. Cross-ref #38 P2 · #38b · #13 typed-ports.
- **#38d PARTS-TABLE per-row MATERIAL popover** (user 2026-07-13). Each parts_table ROW gets a material/colour swatch that opens a popover (`feedback_popup_over_inline`) to set a per-row material/colour OVERRIDE. Model: `PartsTableNode` carries a per-row material (parallel to `rows[]`); `setPartsTableRowMaterial` mutate; emit STAMPS each row's body with its appearance (mirrors the `_stackRef`/`_material` stamp pattern) so the bake renders each row in its own colour (per-INSTANCE, since all rows share one `src` → color-by-source alone gives them the SAME colour). Reuses the per-instance appearance/LUT path — **coordinate with #86 / the BREP per-part-colour fix** (same machinery). Popover kept SELF-CONTAINED in the decoupled `PartsTableCard` (no GraphEditorPane glue — GEP is inline-only, subagents stall on it). Cross-ref #38b · #86 `per_part_viewer_colour`.
- **#75 Auto-layout FORCE params — expose tension + repulsion dials** (user 2026-07-13). The graph-editor auto-layout should run a force-organic pass with (a) CONNECTOR TENSION (link/spring force pulling wired cards together) and (b) CARD REPULSION (charge force pushing overlapping cards apart), and **expose both as adjustable params in the menu** (`feedback_expose_dont_hide` — the slider IS the product). Files: `src/lib/graph/composition-layout.ts` (the layout pass) + the auto-layout menu/toolbar in `GraphEditorPane`. Ties to memory `todo_auto_layout` (heuristic → dagre → force-organic; manual layouts persist). Give sensible defaults + a small range so the user can tune the spread live.
- **#76 Material TEXTURE support** (user 2026-07-16). Add a TEXTURE to the material (beyond flat colour) — a surface map on a part's material appearance. Infra partly exists (`src/lib/shared/viewer/material-textures.ts` + `material-preset` + `part-appearance` + `instance-colors`); wire it through the PROPERTIES card material picker + the render (MF/BREP appearance path). Cross-ref #38d (per-row material) · `per_part_viewer_colour`.
- **#77 Wells = native GRAPH docs · WSON ≈ the composition-graph JSON** (user 2026-07-28, DIRECTION — discuss/plan before building). A well IS a composition-graph document (well-part Calls + placement `Mv` + `warp`), authored/stored/edited through the SAME graph mechanism + engines — NOT a separate WSON schema + runtime translator. The new "WSON" converges to ≈ the graph JSON we already emit. **FIRST STEP:** build basic `.wson` files that MIMIC the current graph JSON (reproduce existing graph parts, e.g. `w2_completion_vert` / `w_multi_string`). **PARTS:** recreate the completion/well elements as parametric VOLUME parts under `well_parts/`, referenced by id DIRECTLY — **NO SVTC `tool_comp` naming-convention parser** (drop `registry.ts`/`COMPLETION_PART_MAP`); an element just references a part id like any Call, so the #42j "unmapped codes" problem dissolves (see #42c). `wsonToGraph` degenerates to a one-time IMPORT of `samples/*.wson`. **OPEN sub-Q:** WSON as a pure graph doc (`.asm.ts`) vs a thin graph-shaped well layer (both 1:1 with the graph). Absorbs #42k. Runtime render already graph-native (#42h). Memory `wells_empty_3d_wiring_gap`.
- **#20 Typed expression outputs** — C explicit annotation · E consumers (`r_sweep.path`, `r_surface_grid`) + ObjectNode emit.
- **#11 Expression-as-builder** — unify the 3 repeats; lacing; 2D preview. · **#31 Visual expression editor** — finish `ExprImperativeBlocks` + `if`; text-DSL mirror.
- **#36 Warp node** — after #940: #12 repeat-as-sweep · #23 varying-section r_sweep · #24/#26 spline as generic point-source · #13 typed-port propagation · #8 repeat editor popover · #86 subpart colors (view-only tint) · #30 custom tabs + local/cloud folders.
- **`w_multi_string_dev` — drop the 9 linear sections, keep ONLY the multi-part warp** (`/plan` #986). Cross-check #38 P2 (collapses the same part) — trim now or once P2 lands.
- **Graph-editor LAYOUT change must NOT rebake** (user 2026-07-12). Repositioning a node is pure visual — geometry unchanged — but it currently re-fires the bake (waste + jank). Key the bake trigger on the STRUCTURAL graph (kinds/params/wires), not node x/y; persist layout without touching the bake hash. Memory `graph_editor_drag_bake_perf`. Verify with a REAL mouse drag. (`/plan` #994)
- **3D orbit · lock the camera lookAt/target to the Z axis (x=y=0)** (user 2026-07-13). For tall Z-down parts (wells) the orbit lets the part drift off-centre because the camera TARGET isn't on the axis — the `lookAt` needs to be set properly. FIX: set the camera `lookAt` / OrbitControls `target` to `(0, 0, z)` — the part's z-centre ON the axis — so the origin stays centred at any depth. Constrain rotation to EITHER azimuthal spin AROUND the Z axis OR tilt around the viewport's HORIZONTAL (screen-x-equivalent) axis — not free orbit. Files: `scene-state.svelte.ts` (cam/target) + `PrimitiveDualScene.svelte` (OrbitControls target + camera-fit). Verify with a real drag on a long part.
- Smaller: **Section card "show cutter"** (view-only translucent wedge overlay) · **In-canvas controls SCREEN-FIXED** (`<HTML fullscreen>` overlay, not world-anchored) · **BUG TF section shows no INNER colours** (`{op:'cutaway'}` must colour exposed faces SECTION_INNER) · **Multi-input SOCKET → compact multi-connector nodes** · **SVG projection smoothness** (silhouette/crease outline, AA strokes; `svg-projection-perf.md` Phase 1) · **#18 r_surface_grid** (unmerged `feat/surface-grid-expr`) · **#21 sweep_demo** (worktree branch, pending apply) · **#17 Loop·x/y toolbar drop**.
- **#998 SPIKE · "WGPU" render tab — WebGPU GPU-rasterizer sibling of B·SVG** (research + scaffold shipped, NOT wired). A future right-pane tab that draws the SAME OCCT true-boundary as B·SVG (silhouette + fill polys from `brep-to-svg.ts`) but GPU-rasterized for **crisp/fast raster vector fills** — NOT 3D per-pixel lighting (that's the MF/TF/BREP mesh tabs). Feature-detect `navigator.gpu` → adapter/device on tab-open, `device.destroy()` on unmount; graceful fallback to B·SVG when WebGPU is absent. Evaluated **onedraw-webgpu** (Zlib; **C11+WGSL, native-only — no browser/WASM build yet**, SDF-shape model ≠ OCCT paths) → **reference only**; build on raw WebGPU + our own WGSL fed by the projector's polylines (add a `format:'polylines'` JSON mode to `/api/brep/svg`). No COEP conflict (a `GPUBuffer` isn't a SAB) — only rule: any asset stays same-origin. Register via `RightPaneTab`/`ALL_TABS`/`ENGINE_TABS.brep` in `embed-config.ts` + a lazy-mount body in `RightPane.svelte`. Design doc: `docs/research/webgpu-onedraw-tab.md`. Scaffold: `src/lib/shared/graph-editor/WebGpuView.svelte` + `webgpu-support.ts` (+ `.test.ts`). Nice-to-have, off the critical path — measure the payoff on a heavy boundary before P2. Cross-ref `trueform-webgpu.md` · `webgpu-slm.md`.

### /design architecture views
Tabs: Tree · C4 · GEP module · Folder tree · Class model · Code graph · Design philosophy + dev toolbar.
- [ ] **API docs from graphify** — `scripts/gen-api-docs.mjs` over `graphify-out/graph.json` (2,132 nodes) → repo API/reference docs in a /design "Docs" tab; regen alongside `gen-design-diagrams.mjs`.

### AI (umbrella #0 — local-first; `docs/plans/ai-master-plan.md`)
- **#0** registry → cloud schema/prompt + local CFG → multishot → feedback corpus → WebLLM (#1/#2/#27/#28/#29 are phases).
- **#29** AI fn library (CI sync test) · **#1** RAG/assist (LEFT `route`/`selectedId` in ctx) · **#27** feedback/RL DB (👍/👎→`turns.jsonl`) · **#2** web-llm (Qwen2.5+XGrammar, default-off) · **#28** synthetic fn-call data.
- [ ] **#3 SPIKE · LiteRT.js — in-browser inference for ONE dynamic page.** ⚠ Verify the API against current docs first (recalled, not verified). Fits the LOCAL-only constraint (`ai_data_residency_local_first`); complements #2 for SMALL task-specific models. **Decide the page first** (best: `/primitives` semantic part search over `ai/rag/parts.jsonl`, today BM25-only). ⚠ **Re-run the cold-classification baseline before adding any model** (silhouette retrieval NOT recommended — cold baseline already 17/18; CLIP collapsed 12/18, `clip_silhouette_collapse`). ⚠ COOP/COEP `require-corp` app-wide — check LiteRT's WASM/WebGPU loading survives it (Manifold main-thread fetch did NOT). Default-OFF, lazy, active-tab-only. Deliverable: go/no-go in `ai-master-plan.md`.
- **RESEARCH · BRep→CSG / BRepNet face-embeddings.** Learned pre-seg + geometry embeddings on BRep topology; our FREE composition-graph sub-part labels = the paper's costliest prereq. Near-term payoff = part RETRIEVAL (feeds #3). ⏳ Phase-0 spike running (`src/lib/server/brep-csg.ts` → `docs/research/brep-csg-spike.md` go/no-go). LATER experimentation surface lives under an EXISTING route (Rule 2 — never a new top-level route).

