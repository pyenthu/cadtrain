# TODO — crisp. Full detail in `/plan` (Gantt) + `docs/plans/*`.
> **Execution:** `batches.md` groups these into issue-and-run batches ("run Batch N to completion").

### Active focus
- **#940/#22 Modularize GraphEditorPane** — Phase 4 (inline, browser-verify each cut): pull the 7 candidates (part-actions · bake-orchestration · expr-actions · keymap/save · canvas-interaction · CSS) onto `controller.svelte.ts` + a `GraphCommand` labeled-undo layer; NodeCard per-type split. (`graph-editor-pane.md`, `modularize-round2.md`, `hierarchical-class-design.md`)
- **#7 Wells `/wells`** — register `g_*` completion parts into the parametric registry; long-string perf; real wells store. (`well-schematic.md`)
- **#42 Wells interface (SVTC WsonApp)** — W-B editor/inspector dock · W-E left toolbar rail. (`wells-interface.md`)
- **#42b Wells → ewells.app PARITY** (`wells-ewells-gaps.md` · `wells-build-architecture.md`):
  - [ ] **A · BUILD ARCH (3D-fast)** — WellBakePool (N own-Manifold workers, per-element parallel bake) · clip-plane cutaway · element libraries (`g_*` via compile+worker) · auto-scale/fit 2D+3D (vertical dia/depth exaggeration + deviated bbox) + manual Dia×/Depth×. Per P0–P5. **Foundation landed** (`d513718`: pool + worker + client seam, unit-tested with a mock worker); the `WellSchematic3D` render-path swap (progressive `$state` map, drop `{#key geomKey}`) is the P2 follow-on.
  - [ ] **B · EDITING (the differentiator)** — mutation+undo · CompletionsEditor (strings table) · SurveyEditor (live re-warp) · inspector-on-select · edit-diagram popovers. **Built but NOT WIRED** (`ab32a2a`): `well-edit-core.ts` + `well-edit-store.svelte.ts` + `CompletionsEditor` + `SurveyEditor` all exist and are unit-tested, but `WellInspectorDock.svelte` has **zero mount sites** — the two editors are reachable only through it, so nothing in `/wells` can open them. Remaining: mount the dock in `WellViewPlaceholder`/`+page.svelte`, wire inspector-on-select, add the edit-diagram popovers.
  - [x] **C · RENDER polish** — 3D center/fill · label banks + anchor option · white scene bg · element-rail icons. SHIPPED (`bbcb32d` + the rail's pre-existing inline icons); verified mounted via `WellViewPlaceholder`. Note: the label-anchor choice lives inside `WellSchematic3D`, not surfaced as a `view-settings` dial.
  - [ ] **D · CHROME** — toolbar actions · ruler MD/TVD · camera presets
- **#42e Wells GRAPH tab + WSON→graph translator** (`src/lib/wells/wson-to-graph.ts`, rungs 1–2 shipped 2026-07-09):
  - [x] **Rung 1–2** — every structural section (`oh` / `cementing` / `ch`) → a `bw_*` Call placed by an `Mv`; mounted as a third `viewMode` in `WellViewPlaceholder` via `GraphEditorPane`'s `seedGraph` (no volume write). Bake-verified: SAMPLE-1 → 9 parts, z 0..1070, outer-r 13.0.
  - [ ] **Rung 3a** — a cut-then-warp PRIMITIVE (skill: primitives first) · **3b** cutaway node in the well graph · **4** warp along the survey · **5** completions as `bw_*` Calls at depth · **6** round-trip graph edits back to WSON.
  - [ ] **BUG — `bw_casing` renders GREEN and the colour cannot be changed.** Reported 2026-07-09; root cause located. `bw_casing` DOES declare `colorOuter:'#460101'` (dark red), but its dep `g_shaft` declares `colorOuter:'#2ecd23'` (green). #86 effective-appearance inheritance (`resolveDepColors` → `effectiveDepColor` in `src/lib/server/part-colors.ts`) resolves the part's BODY colour — its `g_shaft` instances — and hands that up, so the DEP's green wins over the part's OWN explicit `colorOuter`. Editing `bw_casing`'s swatch appears to do nothing because the inherited body colour overrides it. **Fix: a part's own explicit `meta.colorOuter`/`colorInner` must take precedence over the colour inherited from its body's deps** (`effectiveDepColor` should prefer `depColorFromMeta(meta)` when the meta sets one). Affects every `bw_*` built on `g_shaft`, not just casing. Add a part-colors test for "explicit part colour beats inherited dep colour".
  - [ ] **BUG — `bw_casing.top` is declared but IGNORED.** Its body hardcodes `mv(solid, [0,0,0])`, so passing `top` silently places every string at surface. The translator works around it with real `Mv` nodes; either wire `top` in the part or drop the param. Until then every generated casing card shows a call-drift `⚠` for the unpassed arg.
  - [ ] **BUG — closing one `.wson` tab can close several.** Observed 2026-07-09: clicking one `×` removed all four `ws:` workspace tabs whose folder wasn't reopened. `closeTabByKey` looks correct; suspect the workspace restore/persist effect pruning unhydrated `ws:` placeholders.
  - [ ] **NIT** — the generated part id (`w_sample_1_vertical_land_producer`) overflows the bake-pane header; stray-tri badge reads high on very high aspect ratio tubes (topology sound, χ=2).
- **#42c Completion element library (`bw_*`)** — `bw_packer` + a tubing element, then the rest of the completion jewelry. Feeds the wells element registry (A2).
- **#42d Wells CAD-parts API browser + WBG Wizard** — model on [AIDE WBG Wizard](https://aide.mwdstd.com/) (screenshot: `docs/plans/refs/aide-wbg-wizard.png`):
  - **WBG Wizard panel** — section-by-section well bore geometry design: each section = alternating Hole size row + Casing row; standard API sizes shown as clickable pills in a horizontal grid (6½ → 26 in); selected size highlighted teal; curved arrows between rows show clearance relationship between drill bit and casing OD.
  - **CAD-parts picker** — browse/search `/primitives` from the /wells canvas; drop a part into a section with auto-populated params (from `meta.params`); dynamic GUI matching the graph-editor param card (live scrub/edit); wells-specific additions: depth anchor, string assignment, orientation.
  - **Left nav sections**: WBG Wizard · Tubulars · Rig · Trajectory (Auto Design + Cost Model) · Summary (Well Cost · WBG · Report) — section list with + Add section + section count badge.
  - **Auto Design** — trajectory auto-generation given surface location + target TVD/inclination.
  - **Toolbar**: Reset · Apply · Save locally · New · Import · Export · Feedback.
  - See: `docs/plans/wells-cad-parts-browser.md`.

### Open — engines
- **#39 Multi-engine matrix** — BREP client-side. (`docs/architecture/geometry-engines.md`)
- **#46 Graph→TF compiler** — two-tier recompile on a STRUCTURAL hash (topology + param names, not values) so param edits stay real-time; per-SUBPART material (needs color-by-source). (`tf-compile-perf.md`)
- **#51 TF welded-mesh builder** — #50 + NURBS smoothing (`tf.mesh`, `taubinSmoothed`/`laplacianSmoothed`).
- **#50 Arbitrary-section swept-mesh** (subsumed by #51) — transport the 2D section loop along RMF frames + weld → `tf.mesh` (today `tubeMesh` is circle-only).
- **#48/#49 Server compile** — a ⚡client/☁server toggle UI. `#47` parked (per-part WASM `build`).
- **TF double-bake per change** — DIAGNOSED 2026-07-09 (console `[tf]` counter): NATIVE parts single-bake ✅; SERVER-COMPILE parts (composite/warp via `/api/tf/compile`) fire 2–3 `[tf]` bakes on a recipe-SHAPE-changing edit — the `tfRecipeLocal → tfRecipeServer` transition yields two distinct effect keys (`PrimitiveDualCanvas` bake `$effect` key ~L725 embeds the `tfRecipe` object), so it bakes the interim LOCAL recipe AND the resolved SERVER one (+ draft/full). FIX: key the bake on a STABLE structural recipe signature (`JSON.stringify(recipe.instrs)`), or suppress the local-recipe bake while a server compile is pending+imminent. Not a correctness bug (perf only). Overlaps #46.
- **FUTURE: opt-in `compose` toggle (separate vs fused parts)** — expose an explicit per-part/per-list toggle (`list` = separate render parts vs `compose`/`weld` = one solid) instead of the implicit `place()` compose. Mirror in TF (union-fold vs separate instrs).
- **Manifold↔TF bake consistency** (audit `docs/findings/manifold-vs-tf-audit.md`) — (a) converge warp densification (Manifold's fixed `WARP_AXIAL_MAX_ZSPAN` → curvature-adaptive `planAxialStations` like TF); (b) share graph-lowering primitives (`consumed-set`/`stack_ref`/`poly_repeat`) between `graph-to-tf.ts` + `composition-emit.ts`; (c) port bore-extend defect-2 prevention to Manifold hollow sweeps. DEAD CODE: dedup `creaseAwareCornerNormals` (`render-helpers:529` ≈ `trueform-adapter:271`, ~70 LOC — RISKY: differ by a `weldTol` param, not a safe delete).

### Open — editor
- **CONTEXT (2026-07-09) — `g_shaft` now bakes a `sectionCut` into the LEAF, and every `bw_*` part inherits it.** `g_shaft`'s body is `r_revolve({… zSegments: 0 …})` → `sectionCut({az:180, offset:0})`. It is the leaf of `bw_open_hole` · `bw_casing` · `bw_cement` · `bw_prod_tubing`, so the whole well element library — and the generated `/wells` graph — is now half-sectioned. **This is INTENDED: the cutaway must be an ACTUAL PART — a real, capped, manifold CSG solid — not a view-only clip.** Not a code regression; nothing on the TF or warp code path changed. It exposed TWO SEPARATE pre-existing bugs, below. Also note `bw_casing`'s own body ALSO ends in `sectionCut(...)`, so a casing is now cut TWICE — decide whether the leaf or the consumer owns the cut. Measured on `w1_oh_warp`, same graph, fresh dev server: **3D bake = 3,128 tris / 9,384 verts / ⚠237 stray · TF = 762 tris / 2,286 verts, visibly collapsed.** The `⚠48/80 stray` badges on generated well bakes are this, not aspect-ratio noise.
- **BUG (TF) — a CUT part collapses + sprouts stray triangles under warp.** `w1_oh_warp` renders as a thin lens with a visible stray triangle; 4× FEWER verts than the 3D bake of the same graph (2,286 vs 9,384). Fingerprint of TF's `densifyRevolveTree` fast-path (#39) not recognising a `revolve → sectionCut` tree: a cutaway makes the node a NON-revolve, so it falls to the capped non-revolve station limit, under-samples, and `warpMeshJS` bends the (2-z-level) faces as straight chords. `zSegments: 0` on `g_shaft` (Route-C lean revolve, backup `.route-c-backup/`) means there is nothing to fall back on. **FIX: teach `densifyRevolveTree` (`src/lib/shared/tf_examples/execute.ts`) to see THROUGH a cutaway/boolean to the revolve leaves beneath and densify those** (it already walks a revolve-TREE for `revolve.subtract(revolve)`; a cutaway child is the same shape of problem). Rule 25: segmentation at BUILD time, never a post-bake rewrite. Verify: `w1_oh_warp` on the TF tab is smooth, vert count comparable to the 3D bake, 0 strays.
- **BUG (3D BAKE) — warp of a cut part deforms + leaves ~237 stray tris.** SEPARATE from the TF bug above and from the clip-vs-CSG bug below. Root cause already confirmed (see the cut-then-warp entry): `sectionCut` builds its wedge with a bare `new CS([pts]).extrude(zlen)` = only 2 z-levels, while the revolve IS densified via `subdivideProfileAxial(pts, getAxialMaxZSpan(), …)` (`WARP_AXIAL_MAX_ZSPAN=1.5` when a warp is present, `preview/+server.ts:265`). So the subtract's NEW cut faces inherit 2 z-samples → warp bends them as straight chords. **FIX: densify the wedge's Z to match `getAxialMaxZSpan()`. CAVEAT: the naive `extrude(zlen, nDiv, 0)` hits the manifold-3d degenerate-slices bug (nDivisions>0 + twist===0 → "Not manifold", see `cad/CLAUDE.md`); use `extrude(zlen).refineToLength(maxZSpan)` (small, pre-subtract → cheap) or build a welded wedge.** MUST bake-verify manifoldness + that the deformation and the 237 strays are gone.
- **BUG — the CLIENT Manifold worker traps on a cut+warp part, and there is no trap guard.** Symptom the user sees: `preview 400: primitive call failed: emval_methodCallers[caller] is not a function [in g_shaft → r_revolve({… zSegments:0 …})]` on `w1_oh_warp` in `/primitives`. **The 400 is COLLATERAL, not the bug.** `primitive call failed:` is emitted ONLY by `src/routes/api/primitives/preview/+server.ts`; the browser reaches it because `PrimitiveDualCanvas` used to `catch → fetch('/api/primitives/preview')` on any client-bake failure. So: the client Manifold worker traps → silent fallback → the SERVER runs the same geometry, takes the same trap, and its singleton is corrupted for every later bake (that is why a long-lived dev server starts 400ing EVERY part with `table index is out of bounds` / garbage arg types, while prod bakes the identical payload fine). The silent fallback also made the ⚡/☁ badge look one-way: it reports the backend that ACTUALLY produced the mesh, so a client bake that always fell back always read `☁ server`. **Fallback now REMOVED** (2026-07-09) — a client-bake failure is surfaced as an error. Remaining work: (a) **find the actual trap** — server-side `/preview` bakes `w1_oh_warp` fine from a clean singleton (10,188 verts) and does NOT poison it, so reproduce in the BROWSER worker (`bake-worker.ts`); prime suspect is warp × `sectionCut` × `zSegments:0` (see the adaptive-meshing entry below); (b) **give the Manifold paths the trap guard TF already has** — `isTfFatalTrap` + `MAX_WORKER_RESPAWNS` in `tf-bake-client.ts` respawn the TF worker instead of poisoning the session (`3ab5f27`, `a4d5eb0`); the client Manifold worker and the SERVER singleton have NOTHING, so one trap poisons the process. A server-side guard must at minimum re-init `globalThis.__cadtrain_manifold__` after a trap.
- **EXPLORE — is adaptive meshing skipped when a CUT part is warped?** User hypothesis, 2026-07-09, and it fits every symptom above. The two densifiers: **Manifold** — `subdivideProfileAxial(profile, _axialMaxZSpan, _axialMaxSegPerEdge)` (`manifold-mesh.ts:126/178`), applied to the REVOLVE PROFILE via `manifold-helpers.ts:512`, dial `_axialMaxZSpan` set to `WARP_AXIAL_MAX_ZSPAN` when a warp is present. **TF** — `densifyRevolveTree(instr, path)` (`tf_examples/execute.ts:138`), which walks transforms/booleans/unions to reach revolve leaves (comment at :104 names `revolve(outer).subtract(revolve(inner))`), with a capped generic `subdivideAxialAdaptive` for non-revolves. **The question: does either walk see through a `sectionCut`/`cutaway` node to the revolve beneath?** Evidence it does not: `w1_oh_warp` renders 2,286 verts on TF vs 9,384 on the 3D bake — 4× under-sampled — and `g_shaft`'s `zSegments: 0` means there is no baseline sampling to fall back on, so a warp bends 2-z-level faces as straight chords. Instrument BOTH densifiers (log the station count + leaf kind per node) on `w1_oh_warp` before proposing a fix. Rule 25: segmentation at BUILD time, never a post-bake rewrite. Almost certainly the same root as the two warp bugs above AND the client trap.
- **Section card — "show cutter" option** — a view-only toggle on the ✂ section card to render the CUTTING wedge/cube semi-transparent (overlay, not baked) so the author sees what `az`/`offset` removes.
- **BUG INVESTIGATION — a CUT part deforms under warp in 3D BAKE but TF renders it fine** — a part that is ✂ sectioned (cutaway/sectionCut) AND warped along a spline comes out DEFORMED in the Manifold 3D bake, yet the SAME graph renders correctly on the TF tab. Suspected cause: the 3D-bake warp (`warpManifoldAlongSpline` + the `_axialMaxZSpan`/`refine` densification in `bake-worker-core.ts` / `preview/+server.ts`) densifies the ORIGINAL solid but the cutaway's NEW wedge faces (added by `sectionCut`) may lack Z-samples → they bend as straight chords → deformation; TF warps the actual triangulated mesh via `warpMeshJS` + `subdivideAxialAdaptive` (per-triangle Z-stations), so the cut faces get densified and bend smoothly. Investigate: (a) does cut-then-warp vs warp-then-cut order differ between the two paths? (b) does the Manifold warp densify AFTER the cut (so the wedge faces are re-sampled) or before? (c) compare vertex Z-distribution on the cut faces pre/post-warp in both engines. **ROOT CAUSE CONFIRMED (investigation 2026-07-09):** `sectionCut` (`manifold-helpers.ts`) builds its wedge with a BARE `new CS([pts]).extrude(zlen)` = only 2 z-levels, while the revolve IS densified via `subdivideProfileAxial(pts, getAxialMaxZSpan(), …)` (set to `WARP_AXIAL_MAX_ZSPAN=1.5` when a warp is present, `preview/+server.ts:265`). So the subtract's NEW cut faces inherit only 2 z-samples → warp bends them as straight chords → deformation. TF is fine because `densifyRevolveTree` densifies the WHOLE tree (incl. the cutaway child) before warping. **PROPOSED FIX** = densify the wedge's Z to match `getAxialMaxZSpan()` so the cut faces get rings (Rule 25). **CAVEAT:** the naive `extrude(zlen, nDiv, 0)` hits the manifold-3d degenerate-slices bug (nDivisions>0 + twist===0 → "Not manifold", see `cad/CLAUDE.md`); use `extrude(zlen).refineToLength(maxZSpan)` on the wedge (small, pre-subtract → cheap) or build a welded wedge instead. MUST bake-verify manifoldness + that the deformation is gone. ~10 LOC, MEDIUM risk (geometry). Relates to the Manifold↔TF consistency audit (`docs/findings/manifold-vs-tf-audit.md`). EVIDENCE (w1_oh_warp, casing has an az:180 cutaway + warp): 3D bake `docs/plans/refs/cut-warp-3dbake-deformed.png` (casing twisted/collapsed, jagged mesh, 3,342 tris) vs TF `docs/plans/refs/cut-warp-tf-ok.png` (clean, χ=2 watertight, 1,296 tris) — SAME graph.
- **BUG — section `off` should be ROTATIONAL, not axial** — the ✂ section card's `off` input currently shifts the cut wedge in Z (`sectionCut` in `manifold-helpers.ts`: `z0 = bb.min[2] - MARGIN + offset`), which is near-useless since the wedge spans the whole part height. It should be a ROTATIONAL offset — rotate the pie-slice around the Z/drilling axis so the author picks WHICH bearing the cutaway faces (arc from `offset`° to `offset+az`° instead of `0`→`az`). Fix in BOTH `manifold-helpers.ts` sectionCut AND the TF section builder (`graph-to-tf.ts` cutaway + `tf_examples/execute.ts` buildSectionCut) for parity; relabel the card input `off`→`rot`/`bearing`. Small fix. (Screenshot: `docs/plans/refs/section-off-rotational.png`.)
- **BUG — 3D-bake ✂ section clips instead of CSG-cutting (part looks HOLLOW)** — `sectionCut` in `manifold-helpers.ts` appears to clip (no cap) instead of a real `solid.subtract(wedge)`; want a capped, solid-looking cut like the view-only `cutVC`. Check the wedge is closed/manifold + how the section feeds the render (`geo.parts` vs a clip plane). **CONFIRMED again 2026-07-09 in /primitives, with the discriminating evidence: the SAME part on the TF tab shows a PROPER capped CSG cut, while the 3D-bake tab shows an open clip.** So the wedge/section maths is fine (TF builds it from the same graph) — the defect is on the Manifold 3D-bake side alone: either `sectionCut` isn't actually subtracting, or the cut result reaches the viewer as a clip plane / uncapped shell rather than the capped `cutVC` geometry. Diff the two builders: `manifold-helpers.sectionCut` vs the TF `{op:'cutaway'}` arm (`graph-to-tf.ts` + `tf_examples/execute.ts` `buildSectionCut`). Pairs with "TF section shows no INNER colours" (the mirror-image gap).
- **In-canvas controls must be SCREEN-FIXED** — the spline-editor's in-canvas control chips (＋pt/−pt/⌗xyz/view selector, via Threlte `<HTML>`) should stay pinned to the viewport as you orbit, not move with the scene. Use a screen-space overlay (`<HTML fullscreen>` / DOM overlay), not an in-scene world-anchored `<HTML>`. Applies to any in-canvas control overlay.
- **BUG — TF section shows no INNER colours** — the TF `{op:'cutaway'}` builder (`graph-to-tf.ts` + `tf_examples/execute.ts`) must colour the wedge-subtract's exposed faces with SECTION_INNER (grey) / body-inner, matching the Manifold `cutVC` / per-part cut arm.
- **Drop-on-wire → auto-splice (insert op into a connection)** — drag an op/node from the toolbar and DROP it onto an existing wire; it BISECTS that wire and inserts itself between the two endpoints automatically (source output → new node input; new node output → original target slot). Needs: wire hit-detection during a node drag (reuse the `.ge-wire-hit` paths from wire-delete), the drop→splice mutation (rewire source→new, new→target per the target slot's kind), and a highlight of the hovered wire. Pairs with the wire-delete `WireRef` model.
- **mv / rot / other ops → accept MULTIPLE inputs (fan-in)** — like the warp `×N` multi-input socket (#31), let `mv` / `rot` / `txfmn` / `cutaway` (and other single-child ops) accept SEVERAL wired solids on one socket → apply the same transform/op to each (emit a list producer, one transformed part per child, like warp's `[warpSpline(a,…), warpSpline(b,…)]`). Model: `children[]` on the single child socket (mirror `warpChildren`/`setWarpChildAt`/`removeWarpChild`); render one `×N` socket; remove via wire-delete. Saves N separate mv nodes when the same move applies to many parts. Reuse the warp multi-input machinery (5127a1b).
- **Warp → single-chip row (max compact)** — after the #31 multi-input collapse, shrink the warp node further to ONE chip row (like the mv/rot icon chips): the `≈` icon centered, the `path` socket on the TOP-MIDDLE of the title, the `×N` solids count + input socket on the LEFT, output on the RIGHT — no separate solids row. Saves the remaining vertical space. (Follow-on to the warp multi-input socket, 5127a1b.)
- **Multi-input SOCKET → compact multi-connector nodes** — TODAY: warp/output use N STACKED socket rows (`solid 1`/`solid 2`/`+ solid`), which grows the card. GOAL: let ONE socket accept MULTIPLE incoming wires so a card stays COMPACT (Output root list, ✂ section, ≈ warp). Model: N wires → `children[]` on the single socket (data model already stores `children[]`). VISUAL: mark multi-input sockets distinctly (elongated ellipse / barred rounded rect) + a count, not N stacked sockets. INLINE GEP work (NodeCard socket render + wire-state routing).
- **#52 Modularize RightPane** — extract per-tab bodies (SVG/GLB/TF-recipe); large component → HMR silently skips it.
- **#61 Material system + MATERIAL CARD** — (b) textures via `meta.texture` (cement/steel/rock); (c) a Material Card (sibling to Properties/Params) authoring color·opacity·texture·preset per PART+SUBPART. (`wells-ewells-gaps.md` §G-MAT)
- **#66 PER-PART MATERIAL POPOVER + bind-a-variable without wiring** — the big editor-UX enhancement (requested 2026-07-09). TWO halves; the first is small, the second is the real bet.
  - **(a) Popover on every part.** Today overriding a part's appearance means dropping a `◑` MaterialNode on the canvas and DRAGGING a wire into the Call card's `material-in` socket (`graph.materialBindings[callId] = matId`). That's heavy for "make this casing red". Give each Call NodeCard a `◑` chip that opens `MaterialEditorPopover` **anchored to the card** (FloatingPanel, per [[feedback_popup_over_inline]]) writing straight to `graph.partAppearance[callId]`. Nearly all the machinery exists: `PartAppearance` + `partAppearance` map + `resolveEffectiveAppearance` (precedence `materialBindings` → `partAppearance` → part default) + the popover itself, which already edits name·OUT/IN colour·preset·opacity·texture. Missing: the chip, the anchor, and routing `onPatch` to `partAppearance` instead of a MaterialNode. Keep the wire path for REUSE (one material bound to many parts) — the popover is the one-off override. Cross-check the #86 viewer-tint gap: `partAppearance` round-trips today but the per-part 3D tint is still deferred, so a popover that appears to do nothing is the same trap as the `bw_casing` green bug (#42e).
  - **(b) Bind a field to a VARIABLE from inside the popover — no physical wire.** The general ask: anywhere you can type a literal, you should be able to pick a param / calculated expr instead, without dragging a node onto the canvas and wiring it. Requires `PartAppearance`'s fields to widen from plain `string|number` to `ArgValue` (`literal | expr | param`) — the union args already use — and the popover to grow the ƒ affordance ([[feedback_keep_fx_button]]): a dropdown of existing `meta.params` + `e.<name>` exprs, plus "create a new param from this value" which declares it in `graph.params` and rewrites the field to `{kind:'param'}` in one action. Wires then become a VIEW of bindings rather than the only way to make them (`collectEdges` already treats `edges` as derived-from-args, so a popover-created binding draws its own wire for free). This is what makes a well of 40 elements authorable — bind every casing's colour to one `p.casingColor` without 40 drags. Pairs with `port-suggest.ts` / autoWireSuggestions and #38's data-driven params; the same "pick, don't drag" affordance should eventually cover ordinary Call args.
- **#20 Typed expression outputs** — C explicit annotation · E consumers (`r_sweep.path`, `r_surface_grid`) + ObjectNode emit.
- **#11 Expression-as-builder** — unify the 3 repeats (list<op>→sketch · list<transform>→repeat); lacing; 2D preview.
- **#31 Visual expression editor** — finish `ExprImperativeBlocks` + `if`; text-DSL mirror.
- **#36 Warp node** — after #940: **#12** repeat-as-sweep · **#23** generalize r_sweep (varying section) · **#24/#26** spline as generic point-source · **#13** typed ports propagation · **#8** repeat editor popover · **#86** subpart colors (view-only tint) · **#30** custom tabs + local/cloud folders.
- **#38 Data-driven params** — **P2 · list<record> table editor** — `ParamsCard` gains "add object/row" to build a list<record> inline (strings table); the well rebuilds with N strings, zero card-wiring. Payoff: `w_multi_string_dev` 18 cards → 1 list param + 1 producer.
- **#64 LOOP node — produce N parts from params + input part(s)** — a graph node that, given a param LIST (list<number> / list<record>) and one or more INPUT part templates, emits ONE instance PER row, binding each row's fields to the template's args (a `for-each` / lacing producer). Generalizes `parts_map` (data→N solids, already producing) + repeat (N copies of one) into a data-driven part factory: e.g. a casing program (list of {od, length, top}) → N `bw_casing` instances placed by depth. Overlaps #11 (expression-as-builder unify) + #38 P2. Design: how a row binds to template args (by name), placement (place/stack/mv-per-row), and the visual (one card, list socket in + part-template socket + N-instances out). Plan: `docs/plans/loop-producer-node.md`.
- **#18 r_surface_grid** (unmerged `feat/surface-grid-expr`) · **#21 sweep_demo** (fix on a worktree branch, pending apply) · **#17 Loop·x/y toolbar drop**.

### /design architecture views
Tabs: Tree · C4 · GEP module · Folder tree (6 layouts) · Class model · Code graph · Design philosophy — plus a dev toolbar (↻ Rebuild diagrams · Run graphify · Build tree).
- [ ] **API docs from graphify** — `scripts/gen-api-docs.mjs` consuming `graphify-out/graph.json` (2,132 nodes) → repo API/reference docs (public exports · endpoint catalog · module responsibilities) in a buried /design "Docs" tab. Regenerate alongside `gen-design-diagrams.mjs`.

### Platform — the graph editor as an embeddable surface
- [ ] **#65 API + SDK: third parties build apps on our graph editor.** The long-term
  bet. `/wells` is the first proof: a domain app is *just* a translator (domain doc →
  composition graph) + the embedded `GraphEditorPane` + a domain canvas + toolbars
  around it. Generalize that into a public seam so someone outside this repo can do the
  same — **connect the engines (Manifold · TrueForm · BREP/OCCT) into something bigger
  than any one of them**. Needs: a stable graph schema + a documented `/api/primitives/*`
  contract (compile · bake · save · list · types) + an SDK wrapping them + `GraphEditorPane`
  mountable against a caller-supplied part id or in-memory graph (today `{id, embed}`
  only, loads from the volume). Precondition for the wells Graph tab (#42e) being a
  *pattern* rather than a one-off.

### AI (umbrella #0 — local-first; `docs/plans/ai-master-plan.md`)
- **#0** registry → cloud schema/prompt + local CFG → multishot loop → feedback corpus → WebLLM (#1/#2/#27/#28/#29 are its phases).
- **#29** AI fn library (one registry, CI sync test) · **#1** RAG/assist (LEFT `route`/`selectedId` in ctx) · **#27** feedback/RL DB (👍/👎 → `turns.jsonl`) · **#2** web-llm (Qwen2.5 + XGrammar, default-off) · **#28** synthetic fn-call data → few-shot/LoRA.

### Parked / reference
- #19 BUG `casing_schematic` "BREP is deleted" — investigate. · #16 `src/lib` reorg.
- Back-pocket / reference-only (not actionable): #DX in-browser page-agent · #PMP pmp-library (C++ subdivision reference, NOT a 3rd WASM kernel) · #4 CAD-gen research (arxiv) · #4b Blender fields · #5 units repo · #7b r_weld_extrude phase angle · #8b expression library · #17b first-bake-slow profiling.
