# TODO — crisp. Full detail in `/plan` (Gantt) + `docs/plans/*`.

### Active focus
- **#940/#22 Modularize GraphEditorPane** — GEP ~4.3k lines. Phases 0–3 shipped (node-kind registry + golden-emit byte-gate + undo/redo + `GraphEditorController` trunk, 2026-07-07). NEXT (Phase 4, inline, browser-verify each cut): pull the 7 candidates (part-actions · bake-orchestration · expr-actions · keymap/save · canvas-interaction · CSS) onto `controller.svelte.ts` + a `GraphCommand` labeled-undo layer; NodeCard per-type split. Diminishing per-step payoff. (`graph-editor-pane.md`, `modularize-round2.md`, `hierarchical-class-design.md`)
- **#7 Wells `/wells`** — LIVE. NEXT: register `g_*` completion parts into the parametric registry; long-string perf; real wells store. (`well-schematic.md`)
- **#42 Wells interface (SVTC WsonApp)** — NEXT: W-B editor/inspector dock · W-E left toolbar rail. (`wells-interface.md`)
- **#42b Wells → ewells.app PARITY** (`wells-ewells-gaps.md` · `wells-build-architecture.md`):
  - [ ] **A · BUILD ARCH (3D-fast)** — WellBakePool (N own-Manifold workers, per-element parallel bake) · clip-plane cutaway · element libraries (`g_*` via compile+worker) · auto-scale/fit 2D+3D (vertical dia/depth exaggeration + deviated bbox) + manual Dia×/Depth×. Per P0–P5.
  - [ ] **B · EDITING (the differentiator)** — mutation+undo · CompletionsEditor (strings table) · SurveyEditor (live re-warp) · inspector-on-select · edit-diagram popovers
  - [ ] **C · RENDER polish** — 3D center/fill · label banks + anchor option · white scene bg · element-rail icons
  - [ ] **D · CHROME** — toolbar actions · ruler MD/TVD · camera presets
- **#42c Completion element library (`bw_*`)** — DONE: casing/cement/open_hole/tubing/hanger/mule_shoe/nipple. NEXT: `bw_packer` + a tubing element, then the rest of the completion jewelry. Feeds the wells element registry (A2).

### Open — engines
- **#39 Multi-engine matrix** — Manifold ✓ · BREP (server ✓, **client TODO**) · TrueForm ✓. NEXT: BREP client-side. (`docs/architecture/geometry-engines.md`)
- **#46 Graph→TF compiler** — LEFT: two-tier recompile on a STRUCTURAL hash (topology + param names, not values) so param edits stay real-time; per-SUBPART material (needs color-by-source). (`tf-compile-perf.md`)
- **#51 TF welded-mesh builder** — LEFT: #50 + NURBS smoothing (`tf.mesh`, `taubinSmoothed`/`laplacianSmoothed`).
- **#50 Arbitrary-section swept-mesh** (subsumed by #51) — transport the 2D section loop along RMF frames + weld → `tf.mesh` (today `tubeMesh` is circle-only).
- **#48/#49 Server compile** — JS + WASM ✓. LEFT: a ⚡client/☁server toggle UI. `#47` parked (per-part WASM `build`).
- **TF double-bake per change (CHECK)** — user reports the TF tab appears to bake/update TWICE for each edit. A "double-build fix" shipped earlier (session_handoff_2026-07-06) so this may be a regression or a different trigger (a reactive `$effect` firing twice — args identity? geoVersion? active-pane gate?). Repro on a param scrub, count `[bake-client]`/TF worker calls. (new, 2026-07-07)
- **TF timing measured wrong (CHECK)** — the TF tab's reported bake timing looks incorrect. Audit the warm/build split in `PrimitiveDualCanvas` (tf branch ~L562-607: `tim.warm`/`tim.build`, `buildMs` main-thread fallback) — likely double-counts the one-time kernel warm, or the double-bake (above) skews it. (new, 2026-07-07)
- **TF `parts_map` builder** — `graph-to-tf.ts` has no `parts_map` case → native-only TF reports "no builder". Add a native builder (or documented fallback). (new, 2026-07-07)
- **Manifold overlapping-parts UNION → separate-parts (DONE 2026-07-07)** — the 3D bake combined top-level outputs with `place()`→`M.compose`, which FUSES spatially-overlapping bodies (a part inside a transparent open-hole) into one connected body (stray tris, "union in the wireframe"); TF never composes so never fused. FIX: `autoPlace` (`primitive-loader.ts` — JS + compiled-text) stashes the pre-compose outputs on `manifold._parts`; `finalizeManifold` meshes EACH top-level output on its OWN Manifold for `parts[]`/`cutParts[]` (mirrors the z-scale→smooth→warp pipeline per part). Single-output parts unchanged. Verified: casing `A` now a complete separate mesh at opacity 0.2, no fusion.
- **FUTURE: opt-in `compose` option (separate vs fused parts)** — the separate-parts fix always keeps top-level outputs independent (correct for "part inside a transparent part"). Some assemblies may WANT a true boolean/topological weld of overlapping outputs (one fused solid) — expose an explicit per-part/per-list **compose** toggle (author picks `list` = separate render parts vs `compose`/`weld` = one solid) instead of the implicit `place()` compose. Mirror in TF (it already has `op:'union'` for list/group — the toggle picks union-fold vs separate instrs). Scope as its own enhancement. (new, 2026-07-07)
- **Manifold↔TF bake consistency** (audit `docs/findings/manifold-vs-tf-audit.md`, 2026-07-07): **⏳ IN PROGRESS** — Manifold emits `geo.parts` (per-part meshes) so both bakers share the correct per-part transparency path (fixes the open-hole clouding + retires `splitMesh`). LEFT once that lands: (a) x-ray slider no-ops on TF parts — multiply `scene.xrayOpacity` into `pOp` (`PrimitiveDualScene:988,1018`); (b) scene sine-warp toggle no-ops on the TF tab (`PrimitiveDualCanvas:367`); (c) converge warp densification (Manifold fixed `WARP_AXIAL_MAX_ZSPAN` → curvature-adaptive `planAxialStations` like TF); (d) share graph-lowering primitives (`consumed-set`/`stack_ref`/`poly_repeat`) between `graph-to-tf.ts` + `composition-emit.ts` (drift risk); (e) bore-extend defect-2 prevention is TF-only — port to Manifold hollow sweeps. DEAD CODE: dedup `creaseAwareCornerNormals` (`render-helpers:508` ≡ `trueform-adapter:271`, ~70 LOC); drop dead `stackAxis` prop; fold/retire the GLB render-time `warp.ts`; fix stale `graph-to-tf:679` comment.

### Open — editor
- **Spline editor overhaul (2026-07-07)** — feeds the multi-part warp trajectory + /wells deviated surveys:
  - **Plane projections** — FLAT ortho views XY / YZ / XZ; dragging a point in one plane must NOT change the locked out-of-plane axis (per-plane point editing).
  - **Grid extent** — size the grid to the spline's control-point bbox (max extent), not a fixed/too-small box.
  - **Zoom-stable handles** — as you zoom OUT, keep line thickness + point dots at constant SCREEN size so points stay visible + selectable (screen-space, not world-space).
  - **In-canvas controls (Threlte `<HTML>`)** — move ＋point / −point AND the option toggles INSIDE the canvas anchored in-scene (not a separate DOM toolbar).
  - **XYZ table popover** — the XYZ button opens a popover above the canvas: a nicely formatted, SCROLLABLE table of control points (edit x/y/z per row).
  - **Trim verbiage** — reduce on-canvas text; move remaining help to the BOTTOM.
- **Section card — "show cutter" option** — a toggle on the ✂ section card to render the CUTTING wedge/cube itself (semi-transparent, as an overlay) so the author can SEE what volume `az`/`offset` removes. View-only (don't bake it into the solid). (new, 2026-07-08)
- **Cross-section cutaway CARD (graph node) — ⏳ IN PROGRESS (subagent, 2026-07-07)** — a placeable cutaway/section MODIFIER node (like the warp card): wire a solid in → emits an AUTHORED angular section cut (`sectionCut(child, {az, offset})` + a new sandbox helper), part of the GRAPH (per-part), not the view-only bake cutaway. Args: `offset` + an **`az` param controlling HOW MUCH to cut** (angular wedge sweep — 0 = none, 180 = half, 360 = full). Under the ops palette menu. Subagent building it on an isolated worktree (mirrors the warp card end-to-end); merge + TF-lowering review pending.
- **Multi-input warp card (#36b) — ✅ MOSTLY DONE 2026-07-07** (commits 0a64ef9 data/emit · 7c665b8 UI · + in-flight bugfix). Wire MULTIPLE solids into one warp → each bends SEPARATELY along the shared spline (casing + transparent open-hole both warp, stay independent, no fusion). Done: `WarpNode.children[]` · list-producer array emit + root-return spread · NodeCard stacked solid sockets + ＋append + per-child × · wire-state `endWireOnWarpSolid` · mutate `setWarpChildAt`/`removeWarpChild` · geom/WireLayer dynamic sockets · **topoOrder + graph-to-tf consumed-set/expansion fixed** (was: Manifold TDZ "Cannot access A before init" + TF left the 2nd solid straight). LEFT: card redesign (path socket → title row + ⚙ opts popover, IN PROGRESS) · browser-verify both bakes on w1_oh_warp.
- **SVG projection — perf + smoothness + transparency (PLAN, deep-dive 2026-07-07)** — the SVG tab projection needs to be (a) much more EFFICIENT (faster emit; today it re-projects/sorts every triangle on the main thread), (b) SMOOTHER (silhouette/edge quality — anti-aliased strokes, fewer facet chords, curve-aware), and (c) respect PART TRANSPARENCY where applicable (a subpart with `appearance.opacity<1` should render see-through in the SVG, mirroring the 3D/TF per-part opacity — depth-ordered fills with alpha). Plan doc: `docs/plans/svg-projection-perf.md` (subagent deep-dive in flight). Engine: `PrimitiveSvgView` + `svg-emit`/`svg-camera` (`src/lib/shared/`).
- **#52 Modularize RightPane** — extract per-tab bodies (SVG/GLB/TF-recipe); large component → HMR silently skips it.
- **#61 Material system + MATERIAL CARD** — (a) opacity/transparency [IN PROGRESS]; (b) textures via `meta.texture` (cement/steel/rock); (c) a Material Card (sibling to Properties/Params) authoring color·opacity·texture·preset per PART+SUBPART. (`wells-ewells-gaps.md` §G-MAT)
- **#63 SVG bake ↔ material** — the SVG render surface doesn't honor materials: (a) per-subpart color classification, (b) `meta.opacity`→`fill-opacity`, (c) SVG `<pattern>` textures. Files: `PrimitiveSvgView.svelte` · `svg-emit.ts` · `RightPane.svelte`. (§G-MAT5)
- **#20 Typed expression outputs** — A/B/D on main. LEFT: C explicit annotation · E consumers (`r_sweep.path`, `r_surface_grid`) + ObjectNode emit.
- **#11 Expression-as-builder** — unify the 3 repeats (list<op>→sketch · list<transform>→repeat); lacing; 2D preview.
- **#31 Visual expression editor** — finish `ExprImperativeBlocks` + `if`; text-DSL mirror.
- **#36 Warp node** — core shipped; build the node + editor card (after #940). Plus: **#12** repeat-as-sweep · **#23** generalize r_sweep (varying section) · **#24/#26** spline as generic point-source · **#13** typed ports propagation · **#8** repeat editor popover · **#86** subpart colors (view-only tint) · **#30** custom tabs + local/cloud folders.
- **#36b Multi-input warp card (warp N solids in one node)** — FEASIBLE (S–M, LOW risk): warp `child`→`children[]` mirroring the repeat/stack path. Steps: schema+hydrate migration (copy RepeatNode) · emit a LIST (single stays bare `warpSpline`) · descriptor inputRefs/sockets · card per-child socket rows (copy the repeat arm) · shared path v1 · golden gate. File:line refs in `/plan` details.
- **#36c Warp trajectory — z-offset placement** — tangent-extension ✓ shipped. LEFT: **(b) wire `originZ` to a caller** — feed a part's z-offset into the warp (`s = z − splineOrigin`) so multiple strings place on ONE spline by depth; default reproduces today's single-part behaviour. GATE: golden untouched (sampler/render change, not emit). Pairs with #36b.
- **#38 Data-driven params** — P1 schema (`ParamSchema` number|record|list) + P3 `parts_map` producer + node-card + `pm_demo` live part all shipped. LEFT: **P2 · list<record> table editor** — `ParamsCard` gains "add object/row" to build a list<record> inline (a strings table); the well rebuilds with N strings, zero card-wiring. Payoff: `w_multi_string_dev` 18 cards → 1 list param + 1 producer.
- **#18 r_surface_grid** (unmerged `feat/surface-grid-expr`) · **#21 sweep_demo** (fix on a worktree branch, pending apply) · **#17 Loop·x/y toolbar drop**.

### /design architecture views
Tabs: Tree · C4 · GEP module · Folder tree (6 layouts) · Class model · Code graph · Design philosophy. Dev toolbar (↻ Rebuild diagrams · Run graphify · Build tree) all shipped 2026-07-07.
- [ ] **Continue GEP modularization** — see #940 Phase 4.
- [ ] **API docs from graphify** — `scripts/gen-api-docs.mjs` consuming `graphify-out/graph.json` (2,132 nodes, now available) → repo API/reference docs (public exports · endpoint catalog · module responsibilities) surfaced in a buried /design "Docs" tab. Regenerate alongside `gen-design-diagrams.mjs`.
- [ ] **`src/volume_backup/`** (~22k LOC, surfaced by the treemap) — decide whether to move/remove it out of `src/`.

### AI (umbrella #0 — local-first; `docs/plans/ai-master-plan.md`)
- **#0** registry → cloud schema/prompt + local CFG → multishot loop → feedback corpus → WebLLM (#1/#2/#27/#28/#29 are its phases).
- **#29** AI fn library (one registry, CI sync test) · **#1** RAG/assist (engine+panel shipped; LEFT `route`/`selectedId` in ctx) · **#27** feedback/RL DB (👍/👎 → `turns.jsonl`) · **#2** web-llm (Qwen2.5 + XGrammar, default-off) · **#28** synthetic fn-call data → few-shot/LoRA.

### Parked / tooling
- **#DX page-agent** — in-browser agent for live Three/Svelte-`$state`/console inspection; back-pocket (current console+Playwright coverage suffices).
- **#PMP pmp-library** — C++ mesh-processing; reference only for subdivision, DON'T adopt as a 3rd WASM kernel.
- #4 CAD-gen research (arxiv 2606.05515) · #4b Blender fields · #5 units repo · #7b r_weld_extrude phase angle · #8b expression library · #16 `src/lib` reorg · #17b first-bake-slow profiling · #19 BUG `casing_schematic` "BREP is deleted".
