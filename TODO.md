# TODO — crisp. Full detail in `/plan` (Gantt) + `docs/plans/*`.
> **Execution:** `docs/plans/batches.md` groups these into issue-and-run batches ("run Batch N to completion").

### Active focus
- **#940/#22 Modularize GraphEditorPane** — NEXT (Phase 4, inline, browser-verify each cut): pull the 7 candidates (part-actions · bake-orchestration · expr-actions · keymap/save · canvas-interaction · CSS) onto `controller.svelte.ts` + a `GraphCommand` labeled-undo layer; NodeCard per-type split. Diminishing per-step payoff. (Phases 0–3 shipped: node-kind registry + golden byte-gate + undo/redo + `GraphEditorController` trunk.) (`graph-editor-pane.md`, `modularize-round2.md`, `hierarchical-class-design.md`)
- **#7 Wells `/wells`** — NEXT: register `g_*` completion parts into the parametric registry; long-string perf; real wells store. (Live.) (`well-schematic.md`)
- **#42 Wells interface (SVTC WsonApp)** — NEXT: W-B editor/inspector dock · W-E left toolbar rail. (`wells-interface.md`)
- **#42b Wells → ewells.app PARITY** (`wells-ewells-gaps.md` · `wells-build-architecture.md`):
  - [ ] **A · BUILD ARCH (3D-fast)** — WellBakePool (N own-Manifold workers, per-element parallel bake) · clip-plane cutaway · element libraries (`g_*` via compile+worker) · auto-scale/fit 2D+3D (vertical dia/depth exaggeration + deviated bbox) + manual Dia×/Depth×. Per P0–P5.
  - [ ] **B · EDITING (the differentiator)** — mutation+undo · CompletionsEditor (strings table) · SurveyEditor (live re-warp) · inspector-on-select · edit-diagram popovers
  - [ ] **C · RENDER polish** — 3D center/fill · label banks + anchor option · white scene bg · element-rail icons
  - [ ] **D · CHROME** — toolbar actions · ruler MD/TVD · camera presets
- **#42c Completion element library (`bw_*`)** — NEXT: `bw_packer` + a tubing element, then the rest of the completion jewelry. Feeds the wells element registry (A2). (Done: casing/cement/open_hole/tubing/hanger/mule_shoe/nipple.)

### Open — engines
- **#39 Multi-engine matrix** — LEFT: BREP client-side. (Manifold ✓ · BREP server ✓ · TrueForm ✓.) (`docs/architecture/geometry-engines.md`)
- **#46 Graph→TF compiler** — LEFT: two-tier recompile on a STRUCTURAL hash (topology + param names, not values) so param edits stay real-time; per-SUBPART material (needs color-by-source). (`tf-compile-perf.md`)
- **#51 TF welded-mesh builder** — LEFT: #50 + NURBS smoothing (`tf.mesh`, `taubinSmoothed`/`laplacianSmoothed`).
- **#50 Arbitrary-section swept-mesh** (subsumed by #51) — transport the 2D section loop along RMF frames + weld → `tf.mesh` (today `tubeMesh` is circle-only).
- **#48/#49 Server compile** — LEFT: a ⚡client/☁server toggle UI. (JS + WASM compile ✓.) `#47` parked (per-part WASM `build`).
- **TF double-bake per change (CHECK)** — user reports the TF tab appears to bake/update TWICE for each edit. A "double-build fix" shipped earlier (session_handoff_2026-07-06) so this may be a regression or a different trigger (a reactive `$effect` firing twice — args identity? geoVersion? active-pane gate?). Repro on a param scrub, count `[bake-client]`/TF worker calls.
- **TF timing measured wrong (CHECK)** — the TF tab's reported bake timing looks incorrect. Audit the warm/build split in `PrimitiveDualCanvas` (tf branch ~L562-607: `tim.warm`/`tim.build`, `buildMs` main-thread fallback) — likely double-counts the one-time kernel warm, or the double-bake (above) skews it.
- **TF `parts_map` builder** — `graph-to-tf.ts` has no `parts_map` case → native-only TF reports "no builder". Add a native builder (or documented fallback).
- **FUTURE: opt-in `compose` option (separate vs fused parts)** — the separate-parts fix always keeps top-level outputs independent (correct for "part inside a transparent part"). Some assemblies may WANT a true boolean/topological weld of overlapping outputs (one fused solid) — expose an explicit per-part/per-list **compose** toggle (author picks `list` = separate render parts vs `compose`/`weld` = one solid) instead of the implicit `place()` compose. Mirror in TF (it already has `op:'union'` for list/group — the toggle picks union-fold vs separate instrs). Scope as its own enhancement.
- **Manifold↔TF bake consistency** (audit `docs/findings/manifold-vs-tf-audit.md`) — geo.parts per-part transparency path SHIPPED (Manifold + TF). LEFT: (a) x-ray slider no-ops on TF parts — multiply `scene.xrayOpacity` into `pOp` (`PrimitiveDualScene:988,1018`); (b) scene sine-warp toggle no-ops on the TF tab (`PrimitiveDualCanvas:367`); (c) converge warp densification (Manifold fixed `WARP_AXIAL_MAX_ZSPAN` → curvature-adaptive `planAxialStations` like TF); (d) share graph-lowering primitives (`consumed-set`/`stack_ref`/`poly_repeat`) between `graph-to-tf.ts` + `composition-emit.ts` (drift risk); (e) bore-extend defect-2 prevention is TF-only — port to Manifold hollow sweeps. DEAD CODE: dedup `creaseAwareCornerNormals` (`render-helpers:529` ≈ `trueform-adapter:271`, ~70 LOC — RISKY: the two differ by a `weldTol` param, not a safe delete). (stackAxis prop + stale graph-to-tf comment already cleaned in Batch 2; warp.ts is live, not dead.)

### Open — editor
- **Spline editor overhaul (2026-07-07)** — feeds the multi-part warp trajectory + /wells deviated surveys:
  - **Plane projections** — FLAT ortho views XY / YZ / XZ; dragging a point in one plane must NOT change the locked out-of-plane axis (per-plane point editing).
  - **Grid extent** — size the grid to the spline's control-point bbox (max extent), not a fixed/too-small box.
  - **Zoom-stable handles** — as you zoom OUT, keep line thickness + point dots at constant SCREEN size so points stay visible + selectable (screen-space, not world-space).
  - **In-canvas controls (Threlte `<HTML>`)** — move ＋point / −point AND the option toggles INSIDE the canvas anchored in-scene (not a separate DOM toolbar).
  - **XYZ table popover** — the XYZ button opens a popover above the canvas: a nicely formatted, SCROLLABLE table of control points (edit x/y/z per row).
  - **Trim verbiage** — reduce on-canvas text; move remaining help to the BOTTOM.
- **Section card — "show cutter" option** — a toggle on the ✂ section card to render the CUTTING wedge/cube itself (semi-transparent, as an overlay) so the author can SEE what volume `az`/`offset` removes. View-only (don't bake it into the solid).
- **Click a connection → delete popover** — clicking a wire (the bezier in `WireLayer.svelte`) opens a small popover anchored at the click point with a "delete" action that removes THAT connection (unwires the target input slot — e.g. clears the arg/child/path back to unwired). Needs: hit-testable wire paths (widen stroke / invisible fat hit-path) + the unwire mutate per slot type.
- **BUG — 3D-bake ✂ section clips instead of CSG-cutting (part looks HOLLOW)** — the `sectionCut` operator in the Manifold 3D bake appears to CLIP the geometry (remove the wedge without CAPPING the exposed cut faces / keeping the interior solid) so the sectioned part reads as hollow instead of a solid cross-section. Expect a real boolean subtract (`solid.subtract(wedge)`) that leaves a capped, solid-looking cut — mirror how the view-only cutaway (`cutVC`) shows a filled red/grey section. Check `sectionCut` in `manifold-helpers.ts` (is the wedge closed/manifold? is the result a proper subtract, or is a clip/half-space applied?) + how the section part feeds the render (`geo.parts` vs a clip plane). (new, 2026-07-08)
- **In-canvas controls must be SCREEN-FIXED (not rotate with the scene)** — the spline-editor's in-canvas control chips (＋pt/−pt/⌗xyz/view selector, added via Threlte `<HTML>`) should stay pinned to the same VIEWPORT position as you orbit/rotate, not move with the 3D scene. Use screen-space overlay (`<HTML fullscreen>` / a DOM overlay), not an in-scene-anchored `<HTML>` at a world position. Applies to any in-canvas control overlay. (new, 2026-07-08)
- **BUG — TF section shows no INNER colours** — a ✂ section in the TF bake renders the revealed cut interior without the inner cut-surface colour (grey/red), unlike the canvas cross-section flag which colours the interior. The TF `{op:'cutaway'}` builder (`graph-to-tf.ts` + `tf_examples/execute.ts`) needs to colour the wedge-subtract's exposed faces with the SECTION_INNER (grey) / body-inner colour, matching the Manifold `cutVC` / per-part cut arm. (new, 2026-07-08)
- **Multi-input SOCKET → compact multi-connector nodes** — let ONE socket accept MULTIPLE incoming wires (where the node supports it) instead of one socket ROW per input, so a card stays COMPACT as you wire more parts (the warp card already grows one row per solid — this replaces that). Applies to the Output (root list), ✂ section, ≈ warp, etc. Model: N wires → `children[]` on the single socket. VISUAL: mark a multi-input node/socket differently — an elongated ellipse OR a rounded-side rectangle with a couple of horizontal bars symbolising "accepts multiple inputs"; the card shows one compact indicator + a count, not N stacked sockets. (new, 2026-07-08)
- **SVG projection — perf + smoothness + transparency (PLAN, deep-dive 2026-07-07)** — the SVG tab projection needs to be (a) much more EFFICIENT (faster emit; today it re-projects/sorts every triangle on the main thread), (b) SMOOTHER (silhouette/edge quality — anti-aliased strokes, fewer facet chords, curve-aware), and (c) respect PART TRANSPARENCY where applicable (a subpart with `appearance.opacity<1` should render see-through in the SVG, mirroring the 3D/TF per-part opacity — depth-ordered fills with alpha). Plan doc: `docs/plans/svg-projection-perf.md`. Engine: `PrimitiveSvgView` + `svg-emit`/`svg-camera` (`src/lib/shared/`).
- **#52 Modularize RightPane** — extract per-tab bodies (SVG/GLB/TF-recipe); large component → HMR silently skips it.
- **#61 Material system + MATERIAL CARD** — LEFT: (b) textures via `meta.texture` (cement/steel/rock); (c) a Material Card (sibling to Properties/Params) authoring color·opacity·texture·preset per PART+SUBPART. (a) opacity/transparency in progress. (`wells-ewells-gaps.md` §G-MAT)
- **#63 SVG bake ↔ material** — the SVG render surface doesn't honor materials: (a) per-subpart color classification, (b) `meta.opacity`→`fill-opacity`, (c) SVG `<pattern>` textures. Files: `PrimitiveSvgView.svelte` · `svg-emit.ts` · `RightPane.svelte`. (§G-MAT5)
- **#20 Typed expression outputs** — LEFT: C explicit annotation · E consumers (`r_sweep.path`, `r_surface_grid`) + ObjectNode emit. (A/B/D on main.)
- **#11 Expression-as-builder** — unify the 3 repeats (list<op>→sketch · list<transform>→repeat); lacing; 2D preview.
- **#31 Visual expression editor** — finish `ExprImperativeBlocks` + `if`; text-DSL mirror.
- **#36 Warp node** — LEFT (after #940): the remaining warp-family items — **#12** repeat-as-sweep · **#23** generalize r_sweep (varying section) · **#24/#26** spline as generic point-source · **#13** typed ports propagation · **#8** repeat editor popover · **#86** subpart colors (view-only tint) · **#30** custom tabs + local/cloud folders. (Core warp node + multi-input card shipped via #36b.)
- **#36c Warp trajectory — z-offset placement** — LEFT: **(b) wire `originZ` to a caller** — feed a part's z-offset into the warp (`s = z − splineOrigin`) so multiple strings place on ONE spline by depth; default reproduces today's single-part behaviour. GATE: golden untouched (sampler/render change, not emit). (Tangent-extension shipped.)
- **#38 Data-driven params** — LEFT: **P2 · list<record> table editor** — `ParamsCard` gains "add object/row" to build a list<record> inline (a strings table); the well rebuilds with N strings, zero card-wiring. Payoff: `w_multi_string_dev` 18 cards → 1 list param + 1 producer. (P1 `ParamSchema` schema + P3 `parts_map` producer + node-card + `pm_demo` live part shipped.)
- **#18 r_surface_grid** (unmerged `feat/surface-grid-expr`) · **#21 sweep_demo** (fix on a worktree branch, pending apply) · **#17 Loop·x/y toolbar drop**.

### /design architecture views
Tabs: Tree · C4 · GEP module · Folder tree (6 layouts) · Class model · Code graph · Design philosophy — plus a dev toolbar (↻ Rebuild diagrams · Run graphify · Build tree).
- [ ] **API docs from graphify** — `scripts/gen-api-docs.mjs` consuming `graphify-out/graph.json` (2,132 nodes, now available) → repo API/reference docs (public exports · endpoint catalog · module responsibilities) surfaced in a buried /design "Docs" tab. Regenerate alongside `gen-design-diagrams.mjs`.
- [ ] **`src/volume_backup/`** (~22k LOC, surfaced by the treemap) — decide whether to move/remove it out of `src/`.

### AI (umbrella #0 — local-first; `docs/plans/ai-master-plan.md`)
- **#0** registry → cloud schema/prompt + local CFG → multishot loop → feedback corpus → WebLLM (#1/#2/#27/#28/#29 are its phases).
- **#29** AI fn library (one registry, CI sync test) · **#1** RAG/assist (engine+panel shipped; LEFT `route`/`selectedId` in ctx) · **#27** feedback/RL DB (👍/👎 → `turns.jsonl`) · **#2** web-llm (Qwen2.5 + XGrammar, default-off) · **#28** synthetic fn-call data → few-shot/LoRA.

### Parked / tooling
- **#DX page-agent** — in-browser agent for live Three/Svelte-`$state`/console inspection; back-pocket (current console+Playwright coverage suffices).
- **#PMP pmp-library** — C++ mesh-processing; reference only for subdivision, DON'T adopt as a 3rd WASM kernel.
- #4 CAD-gen research (arxiv 2606.05515) · #4b Blender fields · #5 units repo · #7b r_weld_extrude phase angle · #8b expression library · #16 `src/lib` reorg · #17b first-bake-slow profiling · #19 BUG `casing_schematic` "BREP is deleted".
