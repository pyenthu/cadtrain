# TODO — crisp. Full detail in `/plan` (Gantt) + `docs/plans/*`.
> **Execution:** `batches.md` groups these into issue-and-run batches ("run Batch N to completion").

### Active focus
- **#940/#22 Modularize GraphEditorPane** — Phase 4 (inline, browser-verify each cut): pull the 7 candidates (part-actions · bake-orchestration · expr-actions · keymap/save · canvas-interaction · CSS) onto `controller.svelte.ts` + a `GraphCommand` labeled-undo layer; NodeCard per-type split. (`graph-editor-pane.md`, `modularize-round2.md`, `hierarchical-class-design.md`)
- **#7 Wells `/wells`** — register `g_*` completion parts into the parametric registry; long-string perf; real wells store. (`well-schematic.md`)
- **#42 Wells interface (SVTC WsonApp)** — W-B editor/inspector dock · W-E left toolbar rail. (`wells-interface.md`)
- **#42b Wells → ewells.app PARITY** (`wells-ewells-gaps.md` · `wells-build-architecture.md`):
  - [ ] **A · BUILD ARCH (3D-fast)** — WellBakePool (N own-Manifold workers, per-element parallel bake) · clip-plane cutaway · element libraries (`g_*` via compile+worker) · auto-scale/fit 2D+3D (vertical dia/depth exaggeration + deviated bbox) + manual Dia×/Depth×. Per P0–P5.
  - [ ] **B · EDITING (the differentiator)** — mutation+undo · CompletionsEditor (strings table) · SurveyEditor (live re-warp) · inspector-on-select · edit-diagram popovers
  - [ ] **C · RENDER polish** — 3D center/fill · label banks + anchor option · white scene bg · element-rail icons
  - [ ] **D · CHROME** — toolbar actions · ruler MD/TVD · camera presets
- **#42h Wells 3D tab → bake through the PRIMITIVES pipeline (3D bake + TF option).** Today `/wells` 3D is the ported SVTC renderer: pure-THREE shells (`solidTubeForRange`/`shellForRange` + JS `warpGeometry`) with Manifold used ONLY for the boolean cutaway (`src/lib/wells/threeD/manifoldCut.ts`). That is the path the wells skill says to REPLACE.
  - **Target:** the well's graph (`wsonToGraph`) bakes exactly like any assembly — `/api/primitives/compile` → client bake worker (`bake-client.ts`/`bake-worker.ts`), which is the ONE place Manifold reliably inits in-browser (main-thread init is blocked by the app-wide COEP `require-corp`; that was the blank-3D root cause).
  - **Engine selector on the 3D tab:** `3D BAKE` (Manifold) | `TF` — same choice `/primitives` offers, driven off the same graph. TF is native-only (no Manifold fallback → blank + reason).
  - **NO FALLBACK** (wells skill): engine fails → surface the error, never a THREE stand-in.
  - Depends on `#42b A` (WellBakePool for per-element parallel bake) for speed; the 2D-SVG default stays the perf lever so opening a tab still does zero CSG.
  - ✅ **The SEAM IS BUILT + PROVEN (2026-07-10, `d08c227`).** `src/lib/wells/well-graph-bake.ts`: `wellBakeSpec()` (PURE — graph → source → params/options, unit-tested) + `bakeWell()` (compile via `POST /api/primitives/compile` with INLINE source, so nothing is ever saved to the volume → bake in the standard client worker). Both reference wells emit with 0 errors and bake to **16 named parts** (`OH_1…CSG_3`, `COMP_1…COMP_7`) with per-element colours; the open hole already carries `opacity 0.15`. `bakeWell` THROWS when the bake yields no separate parts, rather than rendering the fused shell.
  - **What is LEFT:** mount it in `WellViewPlaceholder`'s 3D tab (render `parts[]`, per-element colour/opacity, layer toggles filter the array), delete the pure-THREE shell path, add the TF engine option. Visual — wants a browser.
  - ✅ Perf, measured through the real worker entry point: deviated well **8551 ms → 1443 ms** once the axial ring spacing is derived from the survey (`#65`/`axial-density`). Vertical bakes in ~100 ms.
  - ⚠ **Render `parts[]`, NOT the composed body.** `M.compose` on OVERLAPPING bodies is a UNION (measured: `compose(big, nestedSmall)` ≡ `big.add(small)`), and every well element sits inside the open hole — the vertical well composed to **510 tris / genus 0**, the open hole having swallowed everything. FIXED at the root in `b3f8b14`: `autoPlace` keeps `_parts` and composes LAZILY, so the union never runs unless something genuinely needs one Manifold.
  - ⚠ **Mixed units: radial = INCHES (`od`), axial = METRES (`length`, `Mv` depth)**, while `bw_*` defaults are all authored in inches. A radial param may default; an axial one must be passed or it bakes ~39× too long. Cost us `bw_packer.seal_len` (fixed `c6cbd30`). Memory: `wells_graph_bake_units_and_parts`.
  - Retires `manifoldCut.ts` + `WellSchematic3D.svelte`'s shell path once at parity.
- **#42f Wells EDITING INTERFACE — model it on SVTC's WsonApp; popovers mutate the GRAPH.**
  - **The key decision:** a property popover must edit the **composition graph**, not just the WSON document. The well IS an assembly graph (`wsonToGraph`), so an edit = a graph mutation → re-bake, and it must land on the SAME undo stack as the graph editor. Target `GraphEditorController` (`src/lib/shared/graph-editor/controller.svelte.ts`, which already owns per-pane undo/redo) rather than inventing a second mutation layer. Open question to settle first: is WSON the source of truth (edit WSON → re-translate → new graph) or is the graph (edit graph → WSON becomes an import format)? Round-tripping both ways is the trap.
  - **Surfaces** (mirror SVTC, one popover per concern, popovers-over-inline per `feedback_popup_over_inline`): casing/tubulars · open-hole sections · cement intervals · **completions** (packers/nipples/mule shoes/SSDs/gauges) · perforations · survey/trajectory · well header. Reference: `~/code/SVTC/src/lib/apps/wson/`; cadtrain already ported `WellPopover` + `WellCompPopup` (dblclick edit) + `WellViewControls`.
  - **Track B is BUILT-but-UNWIRED** — `WellInspectorDock.svelte` has zero mount sites. Wire it before building anything new.
  - Needs a browser to verify (Svelte reactivity + 2D/3D propagation). NOT autonomous-agent work.
- **#42g Wells autoscale + directional/straight toggle**
  - ⚠ **Use SVTC's names — these dials already exist upstream.** `xDiaScale` (radial/diameter exaggeration; 2D default 6, 3D uses `xDiaScale * 3` so one slider drives both — `Wson3DView.svelte:53`) · `yScale` (depth scale, default 0.17) · `autoScale` (true ⇒ `yScale` is ignored, a computed `autoYScale` wins — `wsonRender.js:247`) · `directional` (effective only when the well deviates: `hasDir = wellDir.hasDeviation && displayOpts.directional` — `wsonRender.js:228`). Don't coin a second vocabulary.
  - ⚠ **They already persist in WSON**, which we don't model: `meta.display = { displayOpts: {...}, layers: {...} }` (see `~/Desktop/SAMPLE/schematics/01-vertical-land-producer.wson`). `wson.ts` has no `meta.display` — only a passing mention in `src/lib/wells/CLAUDE.md:136`. Extend the model; do not build a competing persistence path.
  - **Autoscale/fit** — there is NO auto-scale anywhere in `/wells` today (grep: `autoScale|autoFit|fitToView` → 0 hits). `diaScale` / `zScale` are manual dials in the display popover. Want: auto-fit on load + on well change (vertical dia/depth exaggeration from the well's own extents; deviated wells fit their bbox), with the manual Dia×/Depth× dials still overriding. Part of `#42b A`.
  - **Directional / straight toggle** — `settings.directional` already exists (`view-settings.ts:38`) but is buried as a `dm-pill` inside the display-menu popover (`WellViewControls.svelte:137`). Promote it to a top-level segmented control beside `2D | 3D | GRAPH`; it changes what you're looking at, not how it's shaded. Straight = ignore the survey (vertical), Directional = follow it.
- **#42e Wells SAMPLE LADDER — simple → complex, testable at every rung.** Each rung is a `.wson` in `src/lib/wells/samples/` that must (1) translate via `wsonToGraph`, (2) emit source that compiles, (3) bake without strays. Verify HEADLESS first (`wson-to-graph.test.ts` + a bake assertion), then eyeball in `/wells` → GRAPH + 3D. Existing `00-one-casing.wson` is rung 0.
  - [x] **S1 · `10-three-open-holes.wson`** ✅ landed `46b29fe`. — 3 telescoping open holes, VERTICAL. No casing, no cement. Pins hole nesting + Mv depth placement alone.
  - [x] **S2 · shipped as `11-vertical-land-producer.wson`** (the SVTC vertical file, verified to carry NO `profile`) ✅ `46b29fe`. Original name/spec below: — the 3 holes of S1 + 3 concentric casing strings, VERTICAL. The reference vertical well. Pins the OH↔casing clearance the `cementDims` annulus math depends on.
  - [x] **S3 · no file — a NEGATIVE test** in `wson-to-graph.test.ts` (an 8.5" hole around a 9.625" casing must throw `/non-positive annulus/`) ✅ `46b29fe`. Original spec below: — S2 + cement in each annulus. Exercises `cementDims` (throws when the hole is not wider than the casing — that guard is the point).
  - [x] **S4 · shipped as `13-vertical-land-producer-deviated.wson`** (SVTC's J-medium survey; S2 and S4 differ ONLY in `profile`) ✅ `46b29fe`. Original spec below: — S2/S3 + **ONE spline warping every element** (single `warpSpline` before the Output, `originZ: 0`, all Mv ids as `children[]` — the `w1_oh_warp` shape). The deviated reference well. **Unblocked by #64**: half-sectioned `bw_*` elements now warp without the bridging triangle.
  - Notes: elements emitted OUTER→INNER (oh, cement, casing) so transparency reads correctly. NO FALLBACK (wells skill) — a well that can't be expressed as a graph must ERROR, not render a stand-in. Authoring these writes **only** `src/lib/wells/samples/` (repo), NOT the shared prod volume — so it is safe for autonomous work, unlike `#42c`.
  - ~~⚠ Two sample globs~~ **FIXED `3ee5ea2`** — one directory (`src/lib/wells/samples/`); `src/routes/wells/samples/` deleted (its 3 files were byte-identical twins). `wson-summary.test.ts` pins the picker: a wrong glob path resolves to `{}` SILENTLY and just empties it. New `samples/*.wson` still need a dev-server restart (eager glob).
- **#42i Wells SAMPLE CORPUS — provenance + the archetype registry.**
  - **Canonical = `~/code/SVTC/.dev-volume/samples/schematics/`** (10 `.wson` + 10 `.meta.json` + `index.json` + `archetypes.json`). `~/Desktop/SAMPLE/schematics/` is an INCOMPLETE derivative: `02-…wson`, `03-….meta.json`, `04-….meta.json` are missing there. Read-only both; **never run git inside `~/Desktop`** (iCloud-synced — corrupts `.git`).
  - The two `01-vertical-land-producer.wson` files are **not the same well**. SVTC's has no `profile` (truly vertical). The Desktop copy is *the same well plus* an 11-station survey building to 38° inc, and its own `.meta.json` still says `trajectory: "vertical"` — the meta is wrong, the geometry is deviated. SVTC also carries the deviated variant properly as `deviated/01-vertical-land-producer-J-medium.wson`. Use SVTC for both rungs; the pair IS the S2→S4 ladder step (identical `oh`/`ch`/`cementing`, differing only in `profile`).
  - **`archetypes.json` is a natural-language → WSON registry** (its own `_comment` calls it "TODO #20 Layer A"): 10 entries of `{slug, title, template, aliases, defaults}`, looked up by slug or alias. This is the layer `#42d` (WBG Wizard / Auto Design) and the local-first AI work (`#0`) both want — a deterministic "make me a horizontal shale producer" without an LLM inventing geometry. `index.json` is the `{wson, meta}` manifest.
  - Each `.meta.json` carries `{archetype, title, description, well{td,pbtd,trajectory,reservoirTop}, components_used, components_missing_from_catalogue, suitable_for}`. `components_used` (32 distinct across the corpus) is the seed list for `#42d`'s catalog picker and for `#42c`'s `bw_*` build order.
- **#42j Wells registry — 3 `tool_comp` keys silently fall back to a plain `g_tube`.** Measured by running all 10 corpus wells through the real `resolveComponent` (`src/lib/wells/registry.ts`): 14 distinct keys, 11 matched, 3 unmatched — `MISC.SIDE_POCKET_MANDREL` (6×, wells 03/06), `liner_hanger_red` (2×, wells 07/08), `MISC.PUP_PERF` (2×, wells 05/10).
  - The first is a **pure aliasing bug, not a missing part**: `ARTIFICIAL_LIFT_SIDE_POCKET_MANDREL → g_side_pocket_mandrel` is already registered, but the corpus writes the key under `MISC.`, and the `CATEGORY` fallback can't rescue it because it keys on `k.split('_')[0]` = `MISC`, which has no entry. One line + one test fixes six real instances.
  - The other two need parts that don't exist (`liner_hanger_red` is the corpus's own `components_missing_from_catalogue`). Separate concern: an unmatched key should be **observable**, not silently a tube — `resolveComponent` already returns `matched:false`, but nothing surfaces it.
- **#42c Completion element library (`bw_*`)** — `bw_packer` + a tubing element, then the rest of the completion jewelry. Feeds the wells element registry (A2). ⚠ Authoring these WRITES THE SHARED PROD VOLUME (`/api/primitives/save`) — never hand to an unattended agent (memory `subagent_shared_volume_2026-06-14`).
- **#42d Wells CAD-parts API browser + WBG Wizard** — model on [AIDE WBG Wizard](https://aide.mwdstd.com/) (screenshot: `docs/plans/refs/aide-wbg-wizard.png`):
  - **WBG Wizard panel** — section-by-section well bore geometry design: each section = alternating Hole size row + Casing row; standard API sizes shown as clickable pills in a horizontal grid (6½ → 26 in); selected size highlighted teal; curved arrows between rows show clearance relationship between drill bit and casing OD.
  - **CAD-parts picker** — browse/search `/primitives` from the /wells canvas; drop a part into a section with auto-populated params (from `meta.params`); dynamic GUI matching the graph-editor param card (live scrub/edit); wells-specific additions: depth anchor, string assignment, orientation.
  - **Left nav sections**: WBG Wizard · Tubulars · Rig · Trajectory (Auto Design + Cost Model) · Summary (Well Cost · WBG · Report) — section list with + Add section + section count badge.
  - **Auto Design** — trajectory auto-generation given surface location + target TVD/inclination.
  - **Toolbar**: Reset · Apply · Save locally · New · Import · Export · Feedback.
  - See: `docs/plans/wells-cad-parts-browser.md`.

### Open — engines
- ~~**ARCH ⭐ · REMOVE the server bake from the main canvas; two explicit tabs `MF_CLIENT` + `MF_SERVER`.**~~ **DONE 2026-07-10 (`5a94a10`, `ff85625`, `cb885a6`).** The main canvas has ONE geometry backend now: the Manifold Web Worker. The old `⚡/☁` `scene.clientBake` toggle, the 💻/☁ BakeMenu row, and the click-to-switch badge are gone. `backend='manifold'` (client, default, only main-canvas path) vs `backend='manifold-server'` (the new **MF_SERVER** tab). NO fallback either way — an unsupported part is an error naming MF_SERVER, not a redirect. **MF_SERVER never bakes on its own:** it shows a "Server bake — click to load" gate, bakes only on an explicit click, against a FROZEN (source,args) snapshot (param edits don't re-fire it), is never auto-selected, and is never restored on reload. **`runBake` no longer server-bakes:** it used to POST the whole assembly to `/preview` and THROW THE MESH AWAY (read only `ok`/`cacheHash`) — that double bake, on Node's single synchronous thread, IS what wedged `/wells` repeatedly. Now `validateGraphBake` → `/api/primitives/compile` (dep-resolve + emit, no WASM). GLB also moved off the server (`bake-preview` → `glb-client.ts`, browser GLTFExporter). Verified in Chrome: opening a 16-element well's GRAPH fires ZERO `/preview` (8× `/source`, 2× `/compile`); MF_CLIENT 5,188 tris, MF_SERVER same on click. TF tab moved directly below MF_CLIENT.
  - [ ] **LEFT — the SVG tab is now the LAST browser-facing `/preview` caller** (`RightPane:335`). It needs `segmentsFloor`, which the client worker doesn't implement — port that to `bake-worker-core` then move the SVG bake onto the worker like the mesh + GLB. (Related: the SVG degenerate-triangle bug, `/plan` #984, may just be the SVG rendering different geometry than the canvas because of this.)
  - [ ] **LEFT — headless server-bake callers** (`vocab/{infer,bake-proposed,regenerate}`, `composition-bake`'s old path) have no browser. Keep a server bake for them but run it OFF the request thread (`worker_threads`) so it can never starve the loop again. `ProfileFn3DCanvas:59` + the `🔄` rebuild path also still hit `/preview` — move them to the worker.
- ~~**#981 poisoner + #980 well-09 trap**~~ **RESOLVED 2026-07-10.** Two separate bugs presenting as one. (a) `preview/+server.ts`'s finalize catch did `return json({…},{status:400})` — a `return` is not a throw, so `withManifoldTrapGuard` never reset the singleton and one real trap made every LATER bake echo its stale message, naming an innocent part (this is why "every well traps" was a false reading — I'd poisoned the dev server by opening well 09 first). Fixed: `if (isManifoldFatalTrap(e)) throw e;`, pinned in `manifold-trap.test.ts`. (b) Well 09/05's "trap in `finalizeManifold`" was NOT a geometry bug — `/preview` hardcoded `WARP_AXIAL_MAX_ZSPAN=1.5` and ignored the survey-derived span, densifying a 3000 m well ~2000× → `RangeError: Invalid string length` (05) and a WASM table-index trap (09). Honouring `axialMaxZSpan` (`5a94a10`) turned both from HTTP 500 into 200. Lesson kept in memory `manifold_trap_poison`: on a suspected poison, restart between samples or you measure the cascade, not the bug.
- ~~**BUG · `M.compose` of a well's parts DESTROYS it — `preview` forces the compose**~~ **FIXED 2026-07-10 (`5a94a10`, root at `b3f8b14`).** `finalizeManifold` read `_parts` only to pick colours, then meshed the COMPOSED body — and every property GET on the lazyPlace proxy except `_parts` forces the compose, which UNIONS the overlapping well elements down to the outer hole alone (well 11: 16 parts → 498 tris). New `finalizeSeparateParts` transforms + meshes each part independently and concatenates render geometries; nothing is ever composed. Fixes BOTH backends (`preview` + `bake-worker-core` share `finalizeManifold`). Well 11: 498 → **5,188 tris, 16 parts survive**. The `preview:308` type guard was also made proxy-aware.
- **BUG · a `{` in `meta.drawingMd` bakes the part to an EMPTY SOLID.** The meta brace-depth walker counts braces **inside string literals**, so a doc string containing `params: {}` (one `{`, one `}`) mis-slices `meta` and the part silently bakes to nothing — the error you get is the misleading *"Bake produced an EMPTY solid — a CSG op removed all geometry. Check the dimensions"*, which sends you hunting a geometry bug that isn't there. Cost ~30 min while authoring `bw_pup_perf` (2026-07-10); the emitted function body was byte-identical to the working `bw_prod_tubing`, only the md differed. **Fix:** make the walker string-aware (skip `'…'`, `"…"`, `` `…` `` and their escapes). Until then any authored `drawingMd` must avoid curly braces. Related memories: `parsemetaparams_nested_brace_bug`, `json_stringify_meta_regex_traps`. Also worth a guard: `save` should reject (or warn) when a part's meta parses to zero params but the source declares some.
- **#42f · Port real geometry into the 4 imported placeholder well elements.** `well_parts/imported/{bw_pup_perf, bw_trsssv_sp, bw_gauge_mandrel, bw_packer_ahr_ahc}` are plain bored tubes (od/wall/length), created 2026-07-10 so wells 05 + 09 stop throwing "unmapped tool_comp" and blanking the GRAPH tab. Each one's `drawingMd` names the SVTC part to port from (`g_trsssv_sp`, `g_gauge_mandrel`, `g_packer_ahr_ahc`; no perforated-pup part exists — `g_tubing_pup` is the closest plain pup). **Do NOT just alias the `g_*` parts**: they are compjson-inferred revolves with an EMPTY `params` schema, i.e. FIXED geometry (`g_trsssv_sp` bakes od 3.56″ × 11.1″ regardless of input, and its own meta warns the inferred length diverges 85% from catalogue), while the WSON translator feeds every element `od` + `length` — aliasing would silently bake the wrong size at the wrong depth span. The port is: lift each `g_*` profile into a **parametric** revolve driven by `od`/`length`. `PLACEHOLDER_PART_IDS` (`wson-to-graph.ts`) is the live set — drop an id from it when its geometry lands, and `wson-to-graph-coverage.test.ts` enforces that. Same fixed-geometry problem blocks `MISC.SIDE_POCKET_MANDREL` (still unmapped → throws).
- ~~**#64 cut+warp bridging triangle**~~ **DONE 2026-07-10 (e6eeee7).** `solid.subtract(wedge)` retriangulates the planar cut faces + discards the wedge's z-rings; fix = `sectionCut` refines the CUT RESULT (needed a `KERNEL_VERSION` bump). ~~**Engine-fix cache hazard**~~ **DONE (N4, `46b29fe`)** — `engine-hash.ts` folds the engine-source digest into both bake keys; a NEW engine module must be added to `ENGINE_GLOBS` (guarded by a test). ~~**`M.compose` fuses overlapping bodies**~~ **FIXED (`b3f8b14`)** — `autoPlace` keeps `_parts` and composes lazily; TS impl + compiled twin stay in lockstep (`lazy-place*.test.ts`). ~~**#981 trap guard**~~ **DONE (`f894f0d`)** — a WASM trap resets the singleton (server) / respawns the worker (client); the trap itself turned out to be over-densification, not a geometry bug (see the #980/#981 line above).
- **#65 Radial-scale + Z-scale as real PARAMS (warp-aware).** Today `xScale`/`zScale` are RENDER-time scene dials (`scene-state.svelte.ts:43,49`; `setRenderZScale`, `render-helpers.ts:78`) applied last, to the whole scene. Wanted: **build-time parameters** — a **radial scale** (exaggerate diameter) and a **Z scale** (stretch depth) — so exaggeration is part of the geometry, per-part, and survives bake/export (Rule: `feedback_expose_dont_hide` — the slider IS the product).
  - **The subtlety, and the whole point:** under a warp, Z scale must apply **along the spline's arc length, not world Z**. `warpManifoldAlongSpline` maps a vertex's z → an arc-length station and places its (x,y) on the local frame (`warp-spline.ts:11-12,196`). So a depth stretch on a deviated well must scale the **arc-length coordinate `s`** before the frame lookup; scaling world z instead would stretch a horizontal lateral by zero and shear the trajectory. Radial scale is the clean case: multiply the in-frame (x,y) offsets.
  - Applies to both engines (Manifold `warpManifoldAlongSpline` + the JS twin `warpMeshJS` used by client/TF).
  - Directly serves `/wells` `#42g` autoscale (a well's dia/depth exaggeration becomes a param, not a scene hack).
  - Headless-verifiable: assert bbox + arc-length monotonicity + that a vertical and a horizontal section stretch by the same factor along the path.
  - ✅ **Model + emit + bake DONE 2026-07-10 (N3, merged `46b29fe`)**: `xDiaScale`/`yScale` on `WarpNode`, arc-length-aware in BOTH engines, defaults 1 → byte-identical (golden test); measured vertical ×2.000 / horizontal ×2.000 along the path (the world-Z bug would give horizontal ≈1.0). **UI NOT WIRED — this is what remains of #65:** no spline-mode toggle in `SplineEditorPopup`, no warp-scale sliders. Visual; wants a browser.
  - Caveat N3 flagged: `warpMeshJS` normals under **non-uniform** scale are frame-rotated but not inverse-transpose-corrected — a shading approximation on strongly anisotropic scales. Positions are exact.
- ~~**Axial warp density is a scale-blind constant**~~ **FIXED 2026-07-10 (`5eb88f4`) + honoured in `/preview` (`5a94a10`).** `WARP_AXIAL_MAX_ZSPAN=1.5` densified a 1070 m casing to ~713 rings for an invisible sag. Now `BakeOptions.axialMaxZSpan` (derived from the survey's min radius of curvature, `sag ≈ h²/(8R)`). ⚠ `minCurvatureStep` returns `radCurvature=999999` as a **SENTINEL, not Infinity** — decide straightness on the turned angle, not `Number.isFinite`.
  - [ ] Follow-on (N5, held): per-segment **curvature-adaptive** subdivision. The J-well's tangent section (535→1070 m, constant 38°) is straight and still gets rings — that's the remaining fat. Blocked on N6 (a sag budget in raw metres over-tessellates a well drawn at a depth-compressing z-scale).
  - [ ] **N6 · Adaptive meshing must be UNIT- and SCALE-aware — introduce units + render scale as first-class.** Today the sag tolerance is `0.4% of the well's largest RADIUS`, and the radius is in **inches** while the curvature radius `R` is in **metres** (the mixed-unit split above). Worse, tessellation should be judged against what the viewer actually SEES: the canvas applies a depth exaggeration (`zScale`/DTX) and an auto-fit, so a 3000 m well is drawn at a z-scale that makes a "smooth" 1 m sagitta invisible — and a curvature computed in raw metres over-tessellates a well nobody can see the curve on. Sag must be evaluated in the **rendered, scaled coordinate frame** (screen/world-after-transform), not raw model units. That needs (a) an explicit unit tag on every length (radial `in` vs axial `m`) rather than the current convention-by-comment, (b) the render transform (`zScale`, DTX remap, fit scale) reachable at BAKE time — Rule 25 says segmentation happens at build time, so the scale must be an INPUT to `axialSpanForSurvey`/`BakeOptions.axialMaxZSpan`, not a post-bake rewrite, and (c) a pixel- or fraction-of-view sag budget instead of a fraction-of-radius one. Blocks a correct N5. Files: `src/lib/wells/axial-density.ts` · `bake-worker-core.ts` (`axialMaxZSpan`) · `scene-state.svelte.ts` (`zScale`) · `buildRemap` (DTX). Memories: `wells_graph_bake_units_and_parts`, `well_schematic_3d_first`. Relates: `/plan` #944, #5 (units repo).
  - [ ] **N6a · A SCALE is not one number — model it as a set.** Today `zScale` is a lone scalar. Introduce distinct, independently-settable scales: `radial_scale` · `x_scale` · `y_scale` · `z_scale` · **`z_axial_scale`**. `z_scale` and `z_axial_scale` are NOT the same thing and must not be conflated: `z_scale` is a plain geometric stretch of the z axis, whereas `z_axial_scale` exaggerates depth ALONG THE WELL'S AXIS (measured depth / the trajectory arclength) — for a deviated well these diverge, and it is the axial one the depth ruler and the sag budget must read. **In practice the two dials we drive are `radial_scale` and `z_axial_scale`** (fat-and-short well schematics: radii exaggerated, depth compressed); x/y/z stay as escape hatches for plain cartesian work. Consequences: the anisotropy means a circle becomes an ellipse, so tessellation cannot be judged on one radius — the sag budget in N6 must use the scale that applies **in the direction of the curvature**, not a scalar. Also decide where the scales live: a render-only transform (cheap, but then build-time segmentation can't see them, violating Rule 25) vs a bake input (correct, but every scale change re-bakes). Lean bake-input for the axial one, render-only for pure viewing stretches.
- ~~**#67 Spline `mode: 'xyz' | 'survey'`**~~ **DONE 2026-07-10 (N3b, `46b29fe`).** `src/lib/cad/survey-to-xyz.ts` — one minimum-curvature `surveyToXYZ`, sandbox-injected. Left divergent ON PURPOSE: `wson-to-graph.ts` + `assemble.ts` still use average-angle (guarantees station chord = ΔMD, load-bearing for the warp's `originZ:0 → s=z=MD` mapping); switching is browser-verifiable only.
- **BUG · SVG render of a WARPED part has a degenerate triangle** (`/plan` #984). Visible on a warped/deviated part in the SVG tab; suspected leftover shared with the TF + Manifold paths. Decide WHERE it is born: bake emits a zero-area tri (prevent at BUILD time, never post-hoc — can't fix genus), OR `svg-emit` projects two 3D verts onto one 2D point, OR the SVG tab renders different geometry (it is the last `/preview` caller). Diagnose by DECODING triangles, never eyeballing. Files: `svg-emit.ts` · `PrimitiveSvgView.svelte` · `render-helpers.ts`.
- **SVG smooth shading — reuse the 3D shading logic, do NOT fork a second renderer** (`/plan` #985). The SVG tab renders flat facet chords; the 3D canvas shades smoothly from `calculateNormals(3,60)` crease-aware normals. Drive the SVG fill/stroke off the SAME logic. **HARD CONSTRAINT (user 2026-07-10): avoid a brand-new duplication of the SVG** — extend `PrimitiveSvgView` + `svg-emit`, don't add a parallel path. RESEARCH first (verify against current docs): WebGL/WebGPU offscreen render-to-texture + silhouette/crease extraction vs pure-CPU projection with per-vertex normal interpolation + gradient fills. Prior art: archived `exporter.ts` (three-svg-renderer FillPass/VisibleChainPass), `docs/plans/svg-projection-perf.md`. Folds with #63 (SVG pattern textures) + the SVG-projection-smoothness item below.
- **BUG · MF_CLIENT bake badge shows `fresh · 0 ms` for a ~6 s bake** (`/plan` #987). REGRESSION from this session's no-server-bake change: `runBake` → `validateGraphBake` → `/compile` returns only `_t.fetch_total`, which the badge's phase-sum explicitly excludes → sums nothing. The real cost (compile + worker bake + transfer) is measured in `PrimitiveDualCanvas` but only `console.log`ged under `__bakeTimings`. FIX: `bake-worker-core` already attaches `{build,mesh,cutaway,finalize,serialize}` to the result and `bake-client` passes it through — add an `onBakeTimings` callback `PrimitiveDualCanvas → RightPane`, show compile + bake separately, and make "cached" mean the BAKE cache (IndexedDB scriptHash hit), not the compile cache.
- **#39 Multi-engine matrix** — BREP client-side. (`docs/architecture/geometry-engines.md`)
- **#46 Graph→TF compiler** — two-tier recompile on a STRUCTURAL hash (topology + param names, not values) so param edits stay real-time; per-SUBPART material (needs color-by-source). (`tf-compile-perf.md`)
- **#51 TF welded-mesh builder** — #50 + NURBS smoothing (`tf.mesh`, `taubinSmoothed`/`laplacianSmoothed`).
- **TF has NO geometry cache — every TF bake rebuilds the mesh.** Manifold (MF_CLIENT) caches at three layers: server script cache (`id+scriptHash`), client compile LRU (`name|source`), and the IndexedDB MESH cache (`scriptHash+params+options` → full serialized geometry, the "✓ cached · Nms" hit). `tf-bake-client` has none of layer 3 — only the warm WASM kernel (`ensureTf`) is amortized, so the full build runs on every bake (a re-open, a param scrub, a cutaway toggle). Give TF an IndexedDB mesh cache keyed on the recipe hash (+ cutaway + params), mirroring `bake-client.ts`'s `bakeCacheKey`/`idbGet`/`idbPut`. Needs a stable recipe hash (the graph→TF `TfRecipe` is JSON — hash the canonicalized form). Matters once TF is a primary surface / for /wells (N concurrent element bakes).
- ~~**BUG ⭐ · a TF trap POISONS every later TF bake until page reload.**~~ **FIXED 2026-07-11 (`tf-bake-client.ts`).** Reproduced on `w_multi_string`. Fix (a)+(b) landed: a worker trap now REJECTS the trapped job and re-dispatches the WAITING job only if it is a DIFFERENT recipe (recipe-signature compare — no thrash); a worker death NEVER latches to the main thread — it drops the dead worker and respawns a fresh WASM realm, and `workerBroken`/main-thread is reserved for a `new Worker()`/`postMessage` throw (no WASM has trapped → gen-0 import is safe). Net: `w_multi_string` trapping blanks ONLY that part; the next part bakes on a clean worker; no reload. **Root cause is a TF-specific ENGINE bug, proven by Node probe 2026-07-11:** the identical COINCIDENT-face union (casing OD == cement ID) builds a VALID solid in Manifold and in BREP/OCCT (clean at every mesh tolerance), while TF alone gives non-manifold garbage at all seg counts and ABORTS at 192 segs. So if a fused multi-string is ever wanted, route that union through Manifold/BREP, never TF. LEFT (optional, defense-in-depth, NOT on the poison path anymore): (c) make `?tfgen` resolvable so a rare genuine transport-then-trap on the main thread can self-heal (`trueform-client.ts` ensureTf/resetTf).
- ~~**BUG · MF_CLIENT hangs with `parameter 0 has unknown type À®`, self-heal never fires.**~~ **FIXED 2026-07-11 (`manifold-trap.ts` + test).** Mirror of the TF poison: a Manifold worker trap (e.g. an over-densified well) corrupts the embind type registry so the NEXT call reads a GARBAGE type name for a valid arg. `isManifoldFatalTrap` didn't match `has unknown type`/`has unbound type`, so `bake-client.ts:197` rejected the job WITHOUT respawning the worker → corrupted heap survived → every later bake repeated it, then hung. Added both signatures to the trap predicate (+ `describeManifoldError` branch) so the worker respawns a fresh realm and retries, exactly like the trap messages it already caught. Pinned in `manifold-trap.test.ts` (11 pass). Note `RightPane.svelte:432` already flagged this string as a known corruption message — it just wasn't wired to respawn.
- **TF unions top-level parts by DEFAULT — drop it (no-compose parity + it is the trap TRIGGER).** `executeTfRecipe` (`tf_examples/execute.ts:585`) `booleanUnion`-folds every top-level part into one solid. That (a) violates the no-compose rule (user 2026-07-11: "no compose unless explicitly requested with an add or a union"), and (b) is exactly what traps `w_multi_string` — 9 concentric coincident-faced tubes → TF mesh-boolean garbage, abort at 192 segs. Fix: emit each unconsumed root output as a SEPARATE part (mirror Manifold's `finalizeSeparateParts`); fold ONLY when the graph carries an explicit `union`/`add`. Files: `graph-to-tf.ts` (line 17, `list/group → {op:'union'}`) + `execute.ts:585`. Evidence-backed by the same Node probe (both exact kernels fuse the coincident geometry correctly; TF does not).
- **`compose` toggle (separate vs fused parts)** — default is now PARTS everywhere (the no-compose bake, `5a94a10`). LEFT: expose the OPT-IN — an explicit per-part/per-list `MF_Compose`/`weld` toggle that fuses to one solid when the author actually wants it (a real union, not the accidental one). Mirror in TF (union-fold vs separate instrs).
- **Manifold↔TF bake consistency** (audit `docs/findings/manifold-vs-tf-audit.md`) — (a) converge warp densification (Manifold's fixed `WARP_AXIAL_MAX_ZSPAN` → curvature-adaptive `planAxialStations` like TF); (b) share graph-lowering primitives (`consumed-set`/`stack_ref`/`poly_repeat`) between `graph-to-tf.ts` + `composition-emit.ts`; (c) port bore-extend defect-2 prevention to Manifold hollow sweeps. DEAD CODE: dedup `creaseAwareCornerNormals` (`render-helpers:529` ≈ `trueform-adapter:271`, ~70 LOC — RISKY: differ by a `weldTol` param, not a safe delete).

### Engines — modularize + BREP parity (planned 2026-07-11, WITH the user)
- **E1 · Carve `src/lib/engines/{manifold,trueform,brep}/` — a peer to `cad/` + `shared/`, all three engines behind ONE interface.** Decided WITH the user 2026-07-11 (chose the peer over a full cad+shared merge — keeps the UI→engine dependency rule, smallest blast radius). Today the engines are scattered with no principle: Manifold in `cad/` (`bake-{client,worker,core}`, `render-helpers`), TF in `shared/` (`tf-*`, `trueform-client`, `tf_examples/`) + `cad/graph-to-tf`, BREP in `server/brep-occt` + `cad/brep-client`. Move each engine's client/worker/core/kernel into its folder; define `engines/types.ts` — a common `Engine`: `compile(graph, params) → Recipe` · `bake(recipe, params, opts) → { geometry, parts[], cutVC }` · `capabilities` (supportsCutaway/warp, native-only). `cad/` keeps geometry + composition-graph domain; `shared/` keeps UI (GraphEditorPane, ParamGrid, popovers). RULE preserved: UI → engines OK, engines → UI never (engines may import cad GRAPH TYPES only). **One engine at a time**, each move headless-verifiable (build + vitest green, byte-identical bake goldens) before the next. This is the home the GraphEditorPane modularization (#940) bakes into.
- **E2 · BREP full 3D-bake parity: server COMPILE → CLIENT worker CREATE (build+mesh) + cutaway. (Overnight-scoped.)** Today BREP is a SERVER-side OCCT singleton (`brep-occt.ts`, `resetOC()` on trap) — not parallel-safe, no client path. Target mirrors the Manifold/TF split: server compiles the recipe (have `brepFromSource`/`extractRevolveProfile`), a **pooled** client Web Worker builds + meshes (opencascade.js runs in-browser; a WARM pool of N OCCT workers, dispatch parts to free ones — NOT a fresh worker per part, init is multi-MB/~seconds). BONUS from the pool: each worker is its own WASM realm → OCCT traps ISOLATE per part (same realm-isolation as the TF/Manifold worker respawns). Then **cutaway** via OCCT `.cut()` half-space box in the worker (`todo_brep_cutaway`). GUARDRAILS for any overnight agents: verify HEADLESS (build + vitest — OCCT runs in Node); NEVER write the shared prod volume. Files: new `engines/brep/{worker,client,core}`, split `server/brep-occt.ts` (pure-recipe vs worker-exec).
  - **WARP is NOT a port — flagged.** Mesh-warp (per-vertex sine) on a BREP solid discards the exactness that is the reason to use BREP. The BREP-native equivalent is a TRUE sweep along the survey spline (`BRepOffsetAPI_MakePipeShell`/`ThruSections`) — the prize for deviated wells (real curved solids, not warped facets), but a NEW capability, its own effort (own night), NOT part of the overnight compile→create→cutaway scope.
- **E3 · Give the Manifold + BREP client workers a warm POOL (not a lone singleton worker).** Both `bake-client.ts` (Manifold) and the future BREP client run ONE worker. A pool (bounded, warm) unlocks concurrent multi-part bakes (wells = N elements) AND per-part trap isolation. Model on the planned WellBakePool (`wells-build-architecture.md` A1). Relates to E2's OCCT pool.

### Open — editor
- **Section card — "show cutter" option** — a view-only toggle on the ✂ section card to render the CUTTING wedge/cube semi-transparent (overlay, not baked) so the author sees what `az`/`offset` removes.
- **Click a connection → delete popover** — clicking a wire (bezier in `WireLayer.svelte`) opens a popover at the click with "delete" that unwires THAT target input slot. Needs: hit-testable wire paths (fat invisible hit-path) + unwire mutate per slot type.
- ~~**BUG — 3D-bake ✂ section clips instead of CSG-cutting**~~ **NOT A BUG (disproven 2026-07-09).** `sectionCut` was always a real capped CSG (`solid.subtract(wedge)` → genus 0, half volume, positive sign, 2 tris on the cut plane). The cap was being DROPPED at render classification — triangles with an unknown `originalID` were discarded instead of bucketed into the body. Fixed in `render-helpers.ts`; guarded by `sectioncut-render.test.ts`.
- **In-canvas controls must be SCREEN-FIXED** — the spline-editor's in-canvas control chips (＋pt/−pt/⌗xyz/view selector, via Threlte `<HTML>`) should stay pinned to the viewport as you orbit, not move with the scene. Use a screen-space overlay (`<HTML fullscreen>` / DOM overlay), not an in-scene world-anchored `<HTML>`. Applies to any in-canvas control overlay.
- **BUG — TF section shows no INNER colours** — the TF `{op:'cutaway'}` builder (`graph-to-tf.ts` + `tf_examples/execute.ts`) must colour the wedge-subtract's exposed faces with SECTION_INNER (grey) / body-inner, matching the Manifold `cutVC` / per-part cut arm.
- **Multi-input SOCKET → compact multi-connector nodes** — let ONE socket accept MULTIPLE incoming wires so a card stays COMPACT (Output root list, ✂ section, ≈ warp). Model: N wires → `children[]` on the single socket. VISUAL: mark multi-input sockets distinctly (elongated ellipse / barred rounded rect) + a count, not N stacked sockets.
- **SVG projection — smoothness** — silhouette/crease-outline extraction, anti-aliased strokes (`stroke-linejoin:round`/non-scaling-stroke), fewer facet chords, curve-aware (Phase 1 of `docs/plans/svg-projection-perf.md`). Engine: `PrimitiveSvgView` + `svg-emit` (`src/lib/shared/`).
- **#52 Modularize RightPane** — extract per-tab bodies (SVG/GLB/TF-recipe); large component → HMR silently skips it.
- **#16 Modularize `src/lib/shared/`** — 50 loose files / 12.3k lines at the root, no theme signal; `graph-editor/` (50 files) is the only subdir that got it right. Pure `git mv` + import rewrite, **no logic change** — one sweep, not file-by-file. Proposed subdirs (placement from actual imports, not names): `viewer/` (PrimitiveDual{Canvas,Scene} · SceneControls · scene-state · warp · vertex-alpha-partition · instance-colors · part-appearance · material-{preset,textures}) · `svg/` (PrimitiveSvgView · CompJsonSilhouette · svg-{emit,reduce,camera,silhouette}) · `engines/` (trueform-{client,adapter} · brep-adapter · tf-{bake-client,worker,worker-core} · `tf_examples/` folds in) · `profiles/` (ProfileFn{Editor,3DCanvas} · ProfilePalette · profile-{fn-compose,presets}) · `types/` (TypeDefiner{Panel,Popover}) · `volume/` (FolderTreeSidebar · CacheBrowser) · `ui/` (NavMenu · ParamGrid · dragNumber · floating-tip). **Two files leave `shared/` outright**: `WellSideNav.svelte` → `src/lib/wells/` (both consumers are `/wells`; not shared), `anthropic-api.ts` → `src/lib/server/` (only 4 *server* endpoints import it; `shared/` is client UI). **LANDMINE**: `src/routes/design/folder-tree.ts` hardcodes **113** `shared/…` paths — a move breaks the /design folder-tree with no compile error, just a wrong diagram. Update it in the SAME commit. Verify: `bun run build` + `bun run test` + open /design's Folder tree tab. Precedent + method: `todo_modularize_k65`; supersedes the parked "#16 `src/lib` reorg" line below.
- **#66/#982 Per-part material/colour popover ON THE NODE CARD — not a table in PROPERTIES.** Today every subpart is a row in the PROPERTIES card (PART/OUT/IN/MATL/OP). On `w_multi_string_dev` that is 10 rows and the card covers HALF the graph canvas — the table grows linearly with part count, exactly where screen space is scarcest (screenshot 2026-07-10). Wanted: click the material/colour chip ON the node card and edit that part in an anchored FloatingPanel (`feedback_popup_over_inline`). Less space, better graph. Folds in bind-a-variable-to-a-param without wiring. Depends on per-SUBPART material (#947, needs color-by-source).
- **#61 Material system + MATERIAL CARD** — (b) textures via `meta.texture` (cement/steel/rock); (c) the per-PART+SUBPART authoring surface — but as the node-card popover of #66/#982 above, NOT a fourth sibling card. (`wells-ewells-gaps.md` §G-MAT)
- **#63 SVG bake ↔ material** — (c) SVG `<pattern>` textures. Files: `svg-emit.ts` · `PrimitiveSvgView.svelte`. (§G-MAT5)
- **#20 Typed expression outputs** — C explicit annotation · E consumers (`r_sweep.path`, `r_surface_grid`) + ObjectNode emit.
- **#11 Expression-as-builder** — unify the 3 repeats (list<op>→sketch · list<transform>→repeat); lacing; 2D preview.
- **#31 Visual expression editor** — finish `ExprImperativeBlocks` + `if`; text-DSL mirror.
- **#36 Warp node** — after #940: **#12** repeat-as-sweep · **#23** generalize r_sweep (varying section) · **#24/#26** spline as generic point-source · **#13** typed ports propagation · **#8** repeat editor popover · **#86** subpart colors (view-only tint) · **#30** custom tabs + local/cloud folders.
- **#38 Data-driven params** — **P2 · list<record> table editor** — `ParamsCard` gains "add object/row" to build a list<record> inline (strings table); the well rebuilds with N strings, zero card-wiring. Payoff: `w_multi_string_dev` 18 cards → 1 list param + 1 producer.
- **#38b PARTS-TABLE CARD — one card, N rows of the SAME part with different params, each row a wire source.** (User request 2026-07-11, ref `docs/plans/refs/parts-table-card.png`.) Today a multi-string well is N copies of the same `bw_*`/revolve node, each a full card (w_multi_string = 18 cards: 9 sketch + 9 revolve, all wired to the ×9 output — see the screenshot). Replace that with ONE table card: **columns = the part's params** (od/wall/length/segments…), **each row = one instance**, and **each row exposes its own output socket (the "node point")** so rows wire to the Output individually — i.e. the row IS the part instance. Visually mirrors the existing PROPERTIES per-part table (PART/OUT/IN/MATL/OP), but for the geometry params, not appearance. This is the graph-native sibling of #38 P2 (P2 builds a `list<record>` param feeding ONE producer; #38b keeps N addressable outputs so each row can be wired/warped/sectioned independently). Emits as N Call instrs sharing a template src (the parts_map machinery already exists — `graph-to-tf.ts` `parts_map`, `#974`). Rows render as SEPARATE parts (no compose — see the no-union rule below). Decide: is a row editable inline (like the sketch coord table) or via a popover per cell (`feedback_popup_over_inline`)? Payoff: w_multi_string 18 cards → 1 table.
- **`w_multi_string_dev` — drop the 9 linear sections, keep ONLY the multi-part warp** (`/plan` #986). The dev part exists to exercise one spline warping N concurrent bodies; its 9 straight `r_revolve` elements (prd/int/srf × CSG/CEM/OH) are noise that inflates the graph, the PROPERTIES table (#66/#982), and the bake. Rebuild as the warp case alone. Cross-check #38 P2 — it would collapse the same part to 1 list param + 1 producer; decide whether to trim now or do it once when P2 lands.
- **#18 r_surface_grid** (unmerged `feat/surface-grid-expr`) · **#21 sweep_demo** (fix on a worktree branch, pending apply) · **#17 Loop·x/y toolbar drop**.

### /design architecture views
Tabs: Tree · C4 · GEP module · Folder tree (6 layouts) · Class model · Code graph · Design philosophy — plus a dev toolbar (↻ Rebuild diagrams · Run graphify · Build tree).
- [ ] **API docs from graphify** — `scripts/gen-api-docs.mjs` consuming `graphify-out/graph.json` (2,132 nodes) → repo API/reference docs (public exports · endpoint catalog · module responsibilities) in a buried /design "Docs" tab. Regenerate alongside `gen-design-diagrams.mjs`.

### AI (umbrella #0 — local-first; `docs/plans/ai-master-plan.md`)
- **#0** registry → cloud schema/prompt + local CFG → multishot loop → feedback corpus → WebLLM (#1/#2/#27/#28/#29 are its phases).
- **#29** AI fn library (one registry, CI sync test) · **#1** RAG/assist (LEFT `route`/`selectedId` in ctx) · **#27** feedback/RL DB (👍/👎 → `turns.jsonl`) · **#2** web-llm (Qwen2.5 + XGrammar, default-off) · **#28** synthetic fn-call data → few-shot/LoRA.
- [ ] **#3 SPIKE · LiteRT.js — in-browser inference to make ONE page dynamic.** Google AI Edge's web runtime for on-device models (LiteRT/`.tflite`), WebGPU-accelerated, interops with TF.js tensors. Timebox to a spike; **verify the API surface against current docs before designing anything** — this is recalled, not verified, and the web-ML runtime landscape moves fast.
  - **Why it fits:** satisfies the binding constraint that runtime AI must be LOCAL (memory `ai_data_residency_local_first`) — no prompt ever leaves the browser. Complements rather than replaces **#2** (web-llm): LiteRT.js is the right tool for **small, task-specific models** (classifiers, embedders, regressors, vision) where web-llm's LLM-shaped runtime is the wrong shape and far too heavy.
  - **Decide FIRST which page, and whether a model is even warranted.** "Dynamic page" is unscoped. Candidate surfaces, best-first: (a) **`/primitives` part search** — a small text/geometry embedder for semantic "find me a part like this" over the RAG corpus (`ai/rag/parts.jsonl`), today BM25-only; (b) **`/vocab` inferred-tab triage** — score/rank compjson→profile inferences instead of the current hand-tuned heuristics; (c) **bake-cost prediction** — a tiny regressor over graph features to warn "this will take 8s" before the user drags a slider; (d) **`/wells` element classification** from a schematic image. NOT recommended: silhouette/shape retrieval — the cold-classification baseline already hit 17/18 (94.4%) with no retrieval at all, and CLIP embeddings collapsed on 12/18 default-param renders (memories `clip_silhouette_collapse`, `docs/FINDINGS.md`, and the `shared/CLAUDE.md` "cold-classification baseline first" pattern). **Re-run the cold baseline before adding any model.**
  - **Blockers / unknowns to settle in the spike:** COOP/COEP is already `require-corp` app-wide for TrueForm pthreads — check LiteRT.js's WASM/WebGPU asset loading survives it (the Manifold main-thread fetch did NOT; memory `expensive_effects_active_tab_only` and the wells skill's COEP notes). Model weights are a build/CDN asset — the strict-CSP + self-contained constraints apply, and weights must ship same-origin. Must be **default-OFF and lazy**, like #2, with a graceful skip when WebGPU is absent. Must not run on a hidden tab (active-tab-only discipline).
  - **Deliverable:** a written go/no-go in `docs/plans/ai-master-plan.md` — which page, which model, measured latency + bundle cost vs the non-ML baseline it must beat. Related: `todo_webgpu_slm`, `todo_ai_function_mapping`, #2.

### Parked / tooling
- **#DX page-agent** — in-browser agent for live Three/Svelte-`$state`/console inspection; back-pocket (current console+Playwright coverage suffices).
- **#PMP pmp-library** — C++ mesh-processing; reference only for subdivision, DON'T adopt as a 3rd WASM kernel.
- #4 CAD-gen research (arxiv 2606.05515) · #4b Blender fields · #5 units repo · #7b r_weld_extrude phase angle · #8b expression library · #17b first-bake-slow profiling · #19 BUG `casing_schematic` "BREP is deleted". (#16 `src/lib` reorg promoted → "Open — editor".)
