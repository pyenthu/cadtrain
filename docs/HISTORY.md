# Shipped-session history (ledger)

Chronological record of shipped work, moved out of the root `CLAUDE.md`
"Current work in flight" section (2026-06-11 editorial pass). Newest first.
Full per-session detail lives in the session-handoff memories
(`session_handoff_*` in the project memory dir) and in git history.
The roadmap source of truth is `/plan` (root CLAUDE.md Rule 19).

---

**Shipped 2026-06-10/11 (pushed, `e0ff295`..`86fb9b2`):** **GraphEditorPane polygon + loop overhaul (#155–#157).**
* **`PolyRepeatNode` as a separate node type** with its own card on the canvas (Params · Bindings · Loop sections). Polygons embed loops via `{kind:'repeat-ref', sourceId}` entries that interleave with literal vertices in any order; each ref splices N points at its row position. Hydrate auto-migrates legacy inline `{kind:'repeat'}` entries to the new shape — pre-#157 files round-trip without data loss.
* **`NPts` auto-injected** as a const at the top of every loop arrow body so users can write `theta = i*tau/NPts` or `i/NPts` (0..1 fraction) directly. Bindings emit AFTER `NPts` so they can reference it; bindings cascade left-to-right per-iteration.
* **Wire sockets**: poly_repeat output (violet, right edge) → polygon's per-row repeat-ref input (violet, left edge at `y = 36 + idx*39 + 18`); NPts input (amber, left edge at `cy=57`) accepts `p.<name>` wire drops.
* **3-state vertex colours** on the SVG popup + right-pane preview: red `#991b1b` (literal — drag), violet `#6d28d9` (parametric — click ƒ), purple `#a855f7` (loop-generated). Loop dots get a darker violet ring.
* **SVG popover** picks up: ⋮⋮ drag grip (move popover anywhere, e.g. over the 3D canvas), ↩ snap-back to anchor, default pin = true, frozen viewBox (toolbar buttons are the only zoom path), click-to-insert mode with edge-hover (green/orange-🚫), click-to-delete mode.
* **Expression popover** gains r/z (or x/y) tab strip + ƒ buttons on loop expression slots + axis labels adapt to consumer polygon's mode via `polyRepeatModeFor()`.
* **Always call `entryIdxForEvalIdx(node, i)`** in per-point UI to map eval-index → entry-index; direct `polygon.points[i]` lookups silently break for repeat-expansions. See memory `entry_idx_eval_idx_gotcha`.

Detail: memories `polygon_repeat_loop_architecture` + `entry_idx_eval_idx_gotcha`.

**Also shipped 2026-06-11 (pushed, `a584ca3`..`e14f00f`):** g_* migration Round 1 + RAG Phase 1 + sidebar UX cluster + many polish items.
* **NaN guard** in `resolveProfile` (`a584ca3`) — clamps non-finite (r,z) coords to 0 so mid-edit expression typos surface as degenerate geometry instead of opaque WASM "Non-finite vertex" crashes. The bake stays alive while the user iterates.
* **g_* Round 1**: `g_spiral` (flat coil disc, 2 interleaved loops), `g_star` (extruded star prism, one loop with conditional `R = i%2===0 ? R_outer : R_inner` binding), `g_collar` (revolved hollow chamfered tube, 5 literal vertices). All graph-authored (`.asm.ts` extension, `meta.graph` + emitted body). Per-part docs at `docs/parts/g_<id>.md`. `g_dt_joint` (Round 1 part 4) DEFERRED (#167) — needs the graph emit path to handle `place([...])` cleanly. See memory `g_star_parts_curated_list`.
* **JSON.stringify meta regex fixes** (`049db80`, `ab788fc`) — `usesOf` + `extractParamsBlock` now both accept JSON-style quoted property names (`"uses":` / `"params":`). Unblocks ALL programmatic build scripts. Audit any new regex extractor for the same trap. See memory `json_stringify_meta_regex_traps`.
* **RAG Phase 1** (subagent B, `f144c81` + `0dc3030`): corpus extractor at `<volume>/ai/rag/parts.jsonl`, `/api/rag/{rebuild,stats}` endpoints, `↻` rebuild button on `/primitives` sidebar with "last refreshed Xm ago" footnote. Prod corpus = 29 records on first rebuild. Phase 2 (BM25 + Claude) IN FLIGHT — subagent crashed mid-task, needs retry. Plan: `docs/plans/rag-prompt-builder.md`.
* **Sidebar UX cluster** (subagent C, `1c1fd88` + `d304403` + `e14f00f`): `A↓` global alphabetical sort toggle (localStorage), `✎` inline rename via `POST /api/primitives/rename` (existing endpoint), broken-refs scan + repair via new `POST /api/rag/scan-refs` — toast after rename surfaces N affected with `[Repair all]` action that rewrites + re-saves each dependent's `src:'<old>'` references.
* **Call-card title hyperlink** (`4d104ff`) — the `<src>` half of `<alias> · <src>` opens that primitive in a new editor tab via `onOpenTab` prop wired from /primitives.
* **Drag-from-sidebar to canvas** (`955d89e`) — sidebar rows are `draggable:true`; drop on the canvas SVG creates a Call seeded with the part's `meta.params` defaults at the cursor's canvas coords (`clientToCanvas` inverts pan + zoom). Cursor:grab affordance, dashed violet drop-zone highlight.
* **mv/rot ƒ buttons** (`85587d2`) now open the same shared expression popover used by polygon vertex / loop r/z / NPts / binding — consistent across every parametric slot. Apply path routes to `setTransformAxisValue`. Header reads `ƒ mv x expression` etc.

Detail: memory `session_handoff_2026-06-11` (full session ledger).

**Shipped 2026-06-09 (pushed, `37ce44a`..`21338a4`):** **K.70 — /primitives multi-tab + GraphEditorPane extraction.** `/primitives` → `/graph-editor` rename (graph editor is the SINGLE editor); GraphEditorPane component extracted from the page so /primitives mounts N tabs directly; ⚙/+ flowbite-style menus; camera bbox-center; layout overflow fix; top Flowbite Navbar REMOVED (navigation = landing page + per-page chrome); sidebar scroll. Detail: memory `session_handoff_2026-06-09`.

**Shipped 2026-06-06 (pushed, `d3b696e`..`9ef94e9`):** **K.69 — vocabulary editor `/vocab` + boolean_modify rule + completion-parts seeds + mule_shoe end-to-end.**
* **41 seeds ingested from SVTC `comp_list.xlsx`** (`d3b696e`) — 57 catalogue rows folded into 41 unique terms (multi-size variants kept in `variants[]`); 39 carry a `compjson_ref` to the matching 2D silhouette. New file: `docs/parts/vocabulary.seeds.json`. Scripts: `scripts/sync-svtc-compjson.ts` + `scripts/ingest-comp-list.ts`. Drawings live at `static/svtc-compjson/*.json` (72 files, ~2.9 MB).
* **`/vocab` rebuilt as the K.68/K.69 vocabulary editor** (`5687806`..`9ef94e9`) — Topology / Browse left tabs; right pane has Inferred / Proposed vertical trapezoidal rail (matches `/primitives` chrome), 30/70 outer split, 40/60 inner split (canvas / params+rule details). Definition + chip groups encapsulated in a **ⓘ Definition & tags popover** so the tab body focuses on params + 3D bake. Promote button moves inline into the title row alongside Bake. Parameters use `ParamGrid` in a `.pg-acc-wrap` accordion (identical to `/primitives` Build tab). Layout contracts captured in `src/routes/vocab/CLAUDE.md` (don't break these — the zoom-loop bug came from `display: block` killing the canvas height contract). New CLAUDE.md: `src/routes/vocab/`, `src/lib/authoring/`, `src/routes/api/vocab/`.
* **2D drawing → 3D inference pipeline** (`ed0b571`) — `src/lib/authoring/compjson-to-profile.ts` reads SVTC compjson half-section drawings (LEFT = section cut / bore; RIGHT = OD silhouette), classifies elements by x-midpoint, transforms the dominant section polyline to `[r, z]` pairs via OD-calibrated scale. Pure deterministic inference, no GenCAD / DeepCAD / ML weights. Tested live on 4 seeds: `mule_shoe` 5-vert chamfered tip, `tubing_pup` / `flow_coupling` plain cylinders, **`nipple_r_landing` auto-captures 4 landing grooves on the OD** as alternating r-bands. Endpoint: `POST /api/vocab/infer?term=<slug>` → polygon + source + bake.
* **`boolean_modify` rule kind — first generative authoring multiplier** (`6bfcde6`..`9ef94e9`) — `src/lib/authoring/proposal-translator.ts` translates `proposed-vocab-entries.json` entries into source. Rule body is a `polygon_inline` primitive + a `modifiers[]` chain of `{op: 'subtract'|'add'|'intersect', shape: {kind, ...}}`. First shape: `tilted_slab` (rectangular slab whose top face is the cut plane, rotated around an axis by an angle, anchored at a Z — carves one half-space). First use: **mule_shoe**'s 45° angled bottom cut on a hollow tube + box top. Future shape slots: `cylindrical_hole_ring` (perforated pups), `thin_slot_ring` (slotted liners), `lateral_pocket` (side-pocket mandrels), `j_slot_grooves` (indexing). Each new shape = a multiplier across many parts.
* **mule_shoe end-to-end as the boolean_modify exemplar** — `docs/parts/proposed-vocab-entries.json` carries the full rich entry: 11 synonyms (WEG · wireline entry guide · beveled shoe · …), 5 function tags (directional_guiding_into_seal_bore · borehole_cleaning · wireline_entry · …), 5 form tags (tube_with_open_bore · beveled_bottom · single_side_45deg_cut · …), variants (`indexing_mule_shoe`), Parveen reference URL. Bakes to 3456 verts · z=9 · r=2 with the slanted cut visible. Endpoints: `POST /api/vocab/bake-proposed?term=mule_shoe` (body `{params?:[]}` for slider-driven re-bake) + `POST /api/vocab/promote-proposed?term=mule_shoe` (writes the full entry into `vocabulary.json`, bumps version, saves `dt_mule_shoe.prim.ts` to the volume, flips seed status to `'promoted'`).
* **Zoom-loop bug fixed** (`9ef94e9`) — the Proposed canvas was magnifying indefinitely after Bake. Root cause: `.bake-card.no-head { display: block }` + child `.bake-body { height: 100% }` resolved to height 0; canvas auto-fit fired; layout reflowed; loop. Fix: `display: flex` column + `flex: 1 1 auto; min-height: 480px;` on the body. Stable-args `$derived.by` (memoised) was also needed for the `args` prop on `PrimitiveDualCanvas` (a fresh array per render re-mounts the canvas → auto-fit loops). Both captured in `src/routes/vocab/CLAUDE.md` layout contracts.

**Shipped 2026-06-05 (pushed):** **Engine deprecation — stdlib → stdstale** (`7102766` + `ef885cc`): `r_extrude` + `r_revolve` + `r_weld_extrude` moved from `src/lib/cad/stdlib/` to `src/lib/cad/stdstale/` (still resolvable so legacy parts keep baking; sidebar renders a separate collapsed `stdstale` group). New parts are authored via the typed-create popup (Extrude Part / Profile Part / Assembly), never by naming `r_*` engines directly. Also K.68 vocabulary pipeline + dp_test session (Rule 24 codified from its anti-pattern) + Rule 23 subagent test specs (`.claude/agents/test-dp-build.md`).

**Shipped 2026-06-01 (pushed, `24f63b7`..`ae88bc0`):** **Archive restructure — stale code → top-level tracked `archive/`.** `src/routes/archive/*`, `/api/{identify,refine,accept,feedback,wells,kb}`, `src/lib/{identify,training,wells,viewer,tools,authoring(old)}`, `src/lib/cad/{assemblies-l4,exporter,file-kinds,pipe,rules}` all moved to `archive/src/...` (git-tracked; invisible to vite/tsc/router). `/api/cache/stats` restored afterward (`ae88bc0`) — Railway healthcheck. `RATE_LIMITED_PREFIXES` emptied. Full move table + revival instructions: `archive/CADTRAIN_CLEANUP.md`. Also FEM Stage 1 (closed-form stress, Rule 22).

**Shipped 2026-05-28 evening session 2 (pushed, `e471021`..`f40d8f3`):** **K.50 sub-step (b) lands end-to-end + new helpers + 20 w_test parts.**
* **`r_extrude` gains `twist (°)` + `divs`** (`c8572bf`). Default 0/12. Conditional internals: `|twist| < 0.001 → extrude(h)` bare (existing parts unaffected); else `extrude(h, divs, twist)` morphing. Sidesteps the manifold-3d 3.4.1 nDivisions>0-with-zero-twist degenerate-slice "Not manifold" bug.
* **`r_weld_extrude` straight + twist paths corrected** (`e471021`) + **gains `segments`** (`110269c`, default 32 range 4–256). Architecture reservation stays: future hand-wound rail-weld variant for shapes CrossSection can't natively make.
* **`csg-2d.ts` helpers** (`c909ee2` + `7d4178f`): `ext(crossSection, h, twist?, divs?)` wraps the twist-aware terminal extrude; `resample(points, n)` densifies a closed polygon perimeter via cumulative arc-length walk. Both injected into the part sandbox alongside `cs` + `extrude_csg`.
* **All 10 volume parts in `basic/extruded/` updated** (2 bulk scripts in `/tmp/`): twist + divs + segments dials exposed; every `resolveProfile(...)` wrapped in `resample(..., p.segments)`; CSG chains wrapped with `ext()`; `extrude_csg` calls gain trailing `, p.twist, p.divs`.
* **Smooth-shade gate** (`5582c58` + `c8572bf`). `PrimitiveDualCanvas` computes `smoothShade = id === 'r_weld_extrude' || (id === 'r_extrude' && |args[2]| > 0)` and threads it through to `PrimitiveDualScene` where the three live `MeshPhongMaterial` instances use `flatShading={!smoothShade}`. Twisted prisms read smooth (baked `calculateNormals(3, 60)` per-vertex normals) while cubes/hex/everything else keeps the existing flatShading=true look. See `src/lib/cad/CLAUDE.md` "non-planar twisted quads" + memory `flatshading_twisted_quad_smoothshade_gate`.
* **Stdlib `/source` proxy bypass** (`43f17df`). The source endpoint was in `VOLUME_PROXY_PATHS` so local edits to stdlib (`r_*`) were invisible in local dev until pushed (Railway redeploy cycle). Hook bypasses the proxy when `isStdlib(name)` is true. Volume parts continue to proxy. Memory `stdlib_source_proxy_masks_local_edits`.
* **20 `w_test_*` primitives seeded** into `basic/test_primitives/` via `/tmp/seed_w_tests.ts`. 5 add · 5 subtract · 3 intersect · 3 place · 4 mixed (3-op). Every test mixes ≥ 2 of {r_revolve, r_extrude, r_weld_extrude}; 4 mix all three. All build OK end-to-end. Memory `w_test_primitives_seeded`.
* **Editor + sidebar UI**: ProfileFnEditor — Save button now full-width top row, Save-as + Cancel share row 2, 18px column gap (`c909ee2`). Sidebar trash button moved from RIGHT (faded `×`) to LEFT (always-visible Heroicons SVG, `#999` idle → red on hover; CSS `:has(.prim-trash)` reserves a 22px left gutter ONLY on editable rows so non-editable file names don't shift) (`110269c`). Slider deprecation warning fixed — dropped `appearance: slider-vertical` (`3df9fe2`).
* **`/plan` reconciled** — K.50 sub-step (b) marked progress; **K.54 added** (`f40d8f3`): visual Repeat block for `Array.from + place(ring)` at the parts layer — sister to K.47 (profile-layer repeat row), one dimension up.
* **Bench shipped** (in-session, `/tmp/`-scoped): CrossSection-twist 0.85ms vs Welded-twist 1.35ms (1.5×) vs Welded-warp 1.85ms (2.2×) vs **CrossSection-warp 4×** (refineToLength + warp is the killer). For K.50(b)' hand-wound variant: justification is geometric freedom (per-v r/x/y, helical threads), NOT speed. **Emscripten verdict**: ~0.6ms theoretical / ~0.2–0.3ms realistic gain — not worth the toolchain complexity until a real workload reveals the need. Memory `bench_extrude_findings`.
* **Inline-profile architecture confirmed** + **2 canonical templates landed** at `primitives/basic/templates/` on the volume: `template_revolve` (inline `[r,z]` rectangle → r_revolve) + `template_extrude` (inline `Array.from` polar n-gon → r_weld_extrude). The 5 architectural rules (one primitive in `uses`, no `resolveProfile` indirection, **r_weld_extrude as the SINGLE extrude primitive going forward** with r_extrude deprecated, universal `segments` dial, Z-down comments) encoded directly in the templates. Memory `inline_profile_templates_2026-05-28`. **Migration of existing extruded volume parts queued** — they currently use `resolveProfile({kind, params})` and will get bulk-inlined; deprecate `primitives/profiles/` volume directory afterward. User's safety budget: "if it breaks it breaks; we can delete if necessary" — no backward-compat shims, do the migration in place.

**Shipped 2026-05-28 evening session 1 (pushed, `8b2cf86`..`83a152e`):** **Subfolders inside completions/<family>/** + **📁+ folder-create button** — `primitive-paths.findPrim` walks a 3rd level; `/api/primitives/list` returns `completionSubfolders` so empty folders surface; save/move regexes accept the nested form. Per-family `📁+` opens a FloatingPanel that POSTs `/api/volume?action=mkdir` (no new endpoint), nested fold per subfolder, move-picker auto-includes them. **Four drill-pipe test parts** at `completions/drill_pipe/test/` on the volume: `dp_test_2_7_8_g105_nc31`, `dp_test_4_5_g105_nc46`, `dp_test_4_5_20_g105_nc50` (Perforator API spec pp 6-7/14-15/16-17) + `dp_test_hwdp_5_spiral` (3 helical wear pads via `r_threads` at wide pitch + 120° rotations). **Visual loop (repeat row) in the profile editor** — D3-join shape without the D3 library: `+ repeat` cmd with `count / x(i) / y(i)` expression slots; `composeSource` emits `Array.from` spread, `parseBody` Phase 2 decomposes the inverse pattern (including ellipse's block-with-calc form, with chained-calc inlining to fixpoint); `parseCalc` fixed to track brace depth (was leaking inner callback consts as top-level). Three polar-pattern samples on the volume: `ngon_v2` (uniform r), `star_v2` (alternating r via i%2), `gear_v2` (sinusoidal radius). **Cartesian + New profile** now scaffolds a centered `{w, h}` rect (was the revolve half-section, threw "≥ 3 [r,z] points"). **`parseBody` literal-array extractor** — `rect`/`l`/`t`/`plus`/`cylinder`/`tube`/`cone`/`barrel`/`drill_pipe_pin` all decompose into editable expression-based moves. **`/plan` reconciled** — K.46 (subfolders + test parts), K.47 (visual loop + polar samples), K.48 (cartesian scaffold + return-array decomp).

**Shipped 2026-05-28 (pushed, `ad76ffc`..`8722a3d`):** **datum layer in `manifold-helpers`** (`ref`/`head`/`tail`/`mate`/`align` + `mv` carries `_refHead/_refTail` under translate) — structured-wiring path for stacking parts via `mv(b, [0,0,tail(a)])`. **Drill-pipe family** on the volume: `dp_box` · `dp_pin` · `dp_joint` (box+pipe+pin stacked via `tail()`) · `dp_stand` (3 joints overlapping). **Z-pan camera slider** in the scene (vertical range → `scene.zFocus`; OrbitControls target + camera both pan along Z). **Sidebar folder glyphs** (Heroicons folder/folder-open, amber) + caret removed + titles black 14/13px antialiased. **Move parts between folders** UI: `/api/primitives/move` (atomic rename via `primitive-paths`; refuses stdlib) + 📁 picker per row. **`dp_box`/`dp_pin` moved basic→`completions/drill_pipe`** on the volume. **Vertical sidebar section tabs** (Primitives / Components / Archive, trapezoidal, vertical-text; Components flattened — families as top-level folders; Profile-builder link moves into Primitives tab). **`place(parts)` instancing helper** → `Manifold.compose` (a purely topological combine, NO boolean union — "connected/placed, not fused"); originalID preserved through compose + cutaway (`scripts/spike_csg_originalid.ts` TEST 5). **`dp_inst_stand`** on the volume — builds `dp_joint` ONCE, places N translated copies via `place()`; geom cost FLAT in joint count (~58ms vs `dp_stand`'s 147ms 3× build + union; `finalize` still scales with triangles — the Phase-2 boundary). **`Repeat × N` construction-tree node** — `recognize-composite` spots the canonical for-loop + `place()` idiom (`RecognizedRepeat`); `ConstructionTree` renders a green ⟳ node with the inner instance as child + Δz badge. **`forge` branch merged into main** (parked `/forge` image→3D scaffold + FAL Hunyuan3D v2 endpoint; needs `FAL_API_KEY` in Railway to actually run). **`/plan` reconciled** — K.32..K.45 (this session) + deferred K.43 (GPU `InstancedMesh`), K.44 (`repeat_with_data` for heterogeneous), K.45 (enter/update/exit reconciliation). Detail: `session_handoff_2026-05-28`.

**Shipped 2026-05-27 evening (pushed, `397d44b`..`3d6df51`):** **color-by-source rendering** — each part colored by its `r_*` source via Manifold's mesh relation (`runOriginalID`/`runIndex`) through CSG; stamped with a hashId (`src/lib/cad/part-id.ts`), LUT built by `analyzeParts` (`src/lib/server/part-colors.ts`), applied in `builder.ts` + the GLB bake. **Per-part outer/inner colors** with two swatches (square=outer, circle=inner) in each part's accordion title → `meta.instanceColors[name]={outer,inner}`. **`drill_pipe_pin` profile cut 9→5 params** (std upset + 45° nose). **NEW `dp_spec_pin`** spec drill-pipe-pin profile (pipeOD/jointOD/wall→ri, flat `jtUpset` shoulder, 45° upset + 5° thread taper, **thread length DERIVED** — terminates at ri+wall) + **`r_threads` `threadType` selector** (NC38/40/46/50, FH — presets form + OD; values APPROX, refine vs API 7-2) — both on the prod VOLUME. **One-click searchable profile selector** (grid, not nested combobox) + **viewport-clamped FloatingPanels**. Fixes: **edge outlines** re-added to the live canvas, `uniqueInstName` add-part collision, **live profile params from the inline descriptor**, **`defaultsDirty`** ("values changed — Save defaults"), **GLB warp-on-load**. Detail: `session_handoff_2026-05-27c`.

**Shipped 2026-05-27 (pushed):** **components product DELETED** — `/components` route, all 12 `/api/components/*` endpoints, `src/lib/cad/components/` (bundle registry + families), `components-l3`, `server/library.ts`, `component-loader.ts`, the recipe chain (`part-recipe`/`primitive-recipe`/`recipe-preview`), `/archive/**/components`, and the `industrial` category — all gone. `/primitives` is now the one CAD UI; nav + landing repointed; e2e specs updated (runes/instance-ops deleted). `builder.ts` kept (live preview render helpers) but detached from the bundle registry. Also: finished the half-applied profile-swap refactor in `PrimitiveView` (▾ selector → `ProfilePalette` popup) + `r_rotate` function-first scaffold.

**Shipped 2026-05-24 (on `main`, pushed):** warp-at-end toggle + no-stretch 1:1 fix (`warp-spline.ts`) · construction-tree view + **BODMAS diagram** (`ConstructionTree.svelte`) · searchable **profile palette** + volume profiles (`/api/primitives/profiles/{list,save}`) · **volume consolidated to 4 dirs** (archive/components/ai/primitives — kb+kb-sources+training_data+eval → `ai/`; see `volume_4dir_layout`) · drag-resizable /primitives sidebar.

**Earlier shipped:** profile popup · Save As · profiles→named params · instantiable components · single canvas (WebGL leak closed) · collapsible sidebar · transform move + ✕ delete · tests→industrial + completions/<family> nested group.

---

## Long-deferred TODOs (by decision — tracked in /plan)

- **A Phase 2 — GPU `InstancedMesh`** (`/plan` K.43). `place(parts)` today concatenates mesh data; `finalize` is O(triangles) (~2s at 12 joints). True `InstancedMesh` = one geometry + N matrices = flat memory/upload. Build when long strings (50+ joints) become felt pain.
- **D Phase 2 — `repeat_with_data(array, fn)`** (`/plan` K.44). Heterogeneous instances (BHA mix of HWDP / drill pipe / stabilizers, per-iteration params from a data array). Build when a concrete varied use case lands.
- **D Phase 3 — enter/update/exit reconciliation runtime** (`/plan` K.45). True D3-style join inside the bake pipeline (only when Phase 2 strains long strings).
- **Warp z-spline** (`todo_warp_popup_and_logic`) — own popup + revisit the suspected interpretation bug. **Parked at user request 2026-05-24.**
- **Profile P3** — custom-function generator profiles (sandbox-eval, async endpoint pre-resolution).
- **WASM** (`todo_wasm_deferred`) — server-render already conceals; revisit only for offline-client-concealed parts.
- **Customize dir / OAuth** (`todo_customize_dir_deferred`, `docs/plans/oauth-identity.md`) — private per-user parts need the SVTC Google-OAuth port first.
- Other: `todo_r_threads_radial_taper`, `todo_cutaway_unify`, `todo_nonmanifold_csg_split`, `todo_per_part_dynamic_editor`, construction-tree P1–P4 (JSON-tree drag/reparent editor).
